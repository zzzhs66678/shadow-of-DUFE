# 东财之影安全审计

最后更新：2026-08-07

## 1. 范围与信任边界

本轮覆盖主站、auth-api、PostgreSQL、小影、Caddy、静态资料目录、浏览器本地数据、文件上传和第三方校园服务。主要攻击者包括匿名机器人、恶意注册用户、骚扰/刷评用户、被盗会话持有者、恶意上传者、被盗管理员，以及被劫持或协议变化的第三方服务。

核心资产：账号与会话、课表/日程/任务、教师与社区内容、第三方授权包、管理员权限、审计记录和备份。

## 2. 已存在并已验证的控制

- 站内会话使用 32 字节随机令牌，数据库只保存带 pepper 的 HMAC 摘要。
- Cookie 使用 `__Host-`、Secure、HttpOnly、SameSite=Lax，无 Domain。
- OAuth state 与浏览器绑定、10 分钟过期且原子消费，回调重放被拒绝。
- 个人同步写入有同源检查、512KB 上限、字段校验、逐用户版本、幂等 mutation 和事务咨询锁。
- PostgreSQL 与应用服务不映射公网端口；容器具备资源限制和日志轮转。
- 小影敏感连接使用 AES-256-GCM，主密钥与数据库分离。
- 主站当前生产依赖审计为 0 个已知漏洞；其余服务需重试独立审计。

## 3. 当前问题清单

| ID | 等级 | 问题 | 影响 | 修复里程碑 | 状态 |
|---|---|---|---|---|---|
| SEC-001 | P0 | 退出账号后本地 `saved` 未按账号隔离 | B 账号可能合并并上传 A 账号的数据 | M1 | 已修复并验证：按匿名/用户分区，退出清当前账号缓存，显式导入 |
| SEC-002 | P0 | 没有真实注册登录 | 无法建立真实身份、重置、风控和社区权限 | M1 | 已实现待数据库验证：Argon2id 注册登录、锁定、单次重置、会话撤销和 UI 测试通过；邮箱验证已实现摘要存储、邮箱绑定、旧令牌废止、单次原子消费和生产调试模式禁用，生产邮件凭据仍缺失 |
| SEC-003 | P0 | 服务端 RBAC、管理员 TOTP 与审计闭环缺真实数据库验收 | 普通用户越权和管理员被盗风险无法满足验收 | M1 | 已实现待数据库验证：普通用户预查询 403、加密 TOTP、独立恢复码摘要、Strict 短期提升 Cookie、跨会话隔离、重放阻断、用户禁用/会话撤销/审计事务及显式初始化已通过 API 测试；真实 PG 并发/回滚/权限与管理 UI 未验证 |
| SEC-004 | P0 | TraceInt 授权码与 Cookie 交换使用明文 HTTP | 链路或局域网 MITM 可窃取第三方会话 | M7 | 未修复，需核对上游 HTTPS 能力 |
| SEC-005 | P1 | auth-api 未进入 CI 安装、审计和测试 | 认证回归可能静默进入发布 | M1/M8 | 已修复并本地验证，待 PR CI 复核 |
| SEC-006 | P1 | 主要限流为进程内或 IP-only | 重启/多实例绕过，不能覆盖账号级刷接口 | M1/M4/M7 | 部分修复：登录/注册/重置已按 IP+规范化账号限流并持久化账号锁定；多实例共享限流仍未实现 |
| SEC-007 | P1 | 小影后台任务缺租约恢复，幂等/预览部分在内存 | 崩溃后任务卡死或远端结果不确定 | M7 | 未修复 |
| SEC-008 | P1 | 小影原始 `error.message` 可回传用户 | 可能暴露内部结构或第三方响应 | M7 | 未修复 |
| SEC-009 | P1 | 仅一份本地数据库备份，未做完整恢复演练 | 主机故障或备份不可用时无法恢复 | M9 | 未修复 |
| SEC-010 | P1 | 直接 IP 的明文 HTTP 入口仍可访问 | 降级与会话混淆风险 | M9 | 未修复 |
| SEC-011 | P1 | 管理、社区与通用附件模型尚不存在 | IDOR、XSS、文件伪造等控制尚无法完整验证 | M1/M4/M7 | 部分修复：头像已完成魔数/MIME/尺寸/像素/体积校验、重编码去 EXIF、会话与同源约束、账号+IP 限流；通用附件与隔离扫描未实现 |
| SEC-012 | P2 | CSP 仍允许 `unsafe-inline` | 社区上线后会放大存储型 XSS 影响 | M7 | 未修复 |
| SEC-013 | P2 | 迁移和运行服务使用同一数据库角色 | 运行时权限超过最小需要 | M7/M9 | 已实现待数据库验证：迁移脚本创建独立非 owner `AUTH_DB_USER`，撤销审计变更/截断和迁移账本权限；本机无 PostgreSQL，尚未执行权限矩阵测试 |
| SEC-014 | P2 | 默认小影邀请码可无限使用 | 邀请码泄漏可批量创建账号 | M7 | 未修复 |

## 4. 实施要求

### 身份与会话

- Argon2id；统一登录错误；密码强度与泄漏密码策略；失败次数、退避和 IP+账号限流。
- 会话创建后轮换；密码重置、账号禁用和高风险管理操作撤销相关会话。
- 一次性令牌只存摘要，短 TTL，单次原子消费；日志中不出现邮箱完整值、Token 或密码。

### 权限与管理

- 每个管理 API 服务端检查角色与提升会话；不存在性与无权限响应不泄漏敏感对象。
- TOTP secret 加密保存，恢复码只存 hash；管理员操作写 append-only 日志。
- `/admin` noindex，普通用户无入口且直接访问始终被服务端拒绝。

### 输入、社区与上传

- 所有字段白名单校验、长度限制、Unicode 规范化、参数化 SQL；不接受任意对象 mass assignment。
- 账号资料更新已按上述要求实现并通过 Mass Assignment、跨站 Origin 和账号作用域 API 测试；社区与管理员对象仍需分别复核。
- 默认纯文本；若支持富文本，只允许严格标签/属性白名单并净化 URL。
- 头像已通过 MIME 与魔数一致性、尺寸/像素/体积限制和服务端 WebP 重编码移除 EXIF；原图不落库。后续通用附件仍须检查扩展名、MIME 和魔数，并在隔离扫描后再公开。
- 点赞、举报、回复、登录和上传按账号、IP、设备与动作分别限流；唯一索引防重复。

### 部署与恢复

- Caddy 安全头、HSTS、CSP nonce/hash、HTTPS 唯一入口；关闭或重定向明文 IP 站点。
- 拆分数据库 owner/migrator/runtime/backup 角色；服务端只拿运行期最小权限。
- 加密异地备份、定期临时库完整恢复、记录 RPO/RTO；回滚不依赖未验证备份。

## 5. 发布安全门禁

- [ ] 所有 P0/P1 已修复并有复现测试。
- [ ] 普通用户访问全部管理接口均被拒绝。
- [ ] A→退出→B 的浏览器场景无任何账号数据串号。
- [ ] 登录、重置、发布、点赞、回复、举报、上传限流可验证。
- [ ] XSS、CSRF、SQLi、IDOR、SSRF、开放重定向、路径遍历、文件伪造、暴力破解、枚举与 Mass Assignment 测试通过。
- [ ] 三个 Node 项目生产依赖审计达到 high/critical 0。
- [ ] 仓库、镜像、日志、前端 bundle 和备份抽查无真实密钥或令牌。
- [ ] 临时库恢复、回滚和审计查询通过。
