# 东财之影正式发布进度

最后更新：2026-08-11

## 当前状态

- 分支：`codex/release-upgrade`
- 当前里程碑：M8/M9 自动化与部署准备。M7 的本地安全切片继续收口；M6 的移动端、无障碍、请求时序、结果窗口和本地性能切片已完成，真实 Web Vitals 保留为 staging 门禁。原生 PostgreSQL 17 CI 已覆盖迁移幂等、基础 ACL/共享限流以及两个真实普通账号的 auth/community HTTP 闭环，但作业尚未推送运行，不能作为已通过证据。
- 已完成提交：`bdfc6d3` 视觉基线、`91a296e` 发布计划、`c16fe40` 账号本地数据隔离、`9d108c4` 凭据认证、`b6514b4` 账号资料管理、`1d46813` 安全头像处理、`9da33ac` 管理员安全底座、`5315960` 管理员 RBAC/API、`d7fc830` 管理员值守台、`ef8675e` 邮箱验证、`7f815f0` 资料检索 API、`3bffd6e` 独立资料档案、`4337290` 社区数据底座、`324c59c` 社区公开读取 API、`452efd0` 社区主题/评论写入、`7e8994e` 社区互动与举报、`89abc3a` 社区通知闭环、`437bc36` 管理员社区治理。
- 生产状态：本轮代码未部署；正式站仍运行既有版本。
- 用户未纳入版本控制的 `Website_Information_Form.doc`、`public/images/dufe-campus-timeprint-v1.webp` 与 `待上传资料/` 保持原样。

## 状态定义

- **已验证**：功能已实现，并有自动化或明确人工验收证据。
- **已实现待凭据**：代码与本地/测试验证完成，只差生产密钥或第三方资质。
- **已实现但测试失败**：代码存在，但当前不能作为可交付能力。
- **未实现**：尚未开始或仅完成设计。
- **外部阻塞**：只能由生产凭据、验证码、实名认证、付费资源或不可逆操作解除。

## 里程碑总览

| 里程碑 | 状态 | 已有证据 | 下一步 |
|---|---|---|---|
| M0 审计与计划 | 已验证 | 仓库、页面、技术栈、部署、工作簿、多角色审计、基线测试与五份执行文档已提交 | 已完成 |
| M1 账号/同步/管理 | 进行中 | 账号本地数据分区；凭据注册登录；Argon2id；失败锁定；一次性重置；资料白名单更新；安全头像；邮箱验证令牌闭环；管理员 TOTP/恢复码/独立提升 Cookie/管理 API/值守台/审计；运行角色配置；auth-api CI | 在 PostgreSQL 17 复核迁移/权限/并发，接生产邮件与校园账号验证 |
| M2 资料检索 | 已实现并本地验证 | 457 条资料实体、独立 API/页面/详情、URL 搜索状态、直接预览下载、异常状态、键盘与 390px 手机路径 | staging 挂载真实资料卷后抽样预览/下载与多宽度 E2E |
| M3 教师/导入 | 已实现并本地验证 | 完整版工作簿 dry-run、私有规范化导入包、稳定教师来源身份、教学班教材版本、事务 apply/幂等/依赖回滚、管理员候选审核、公开教师 API、索引/详情、课表检索入口和登录用户五维评价均已本地验证；源表和生产库未写入 | 在原生 PostgreSQL 17 验证 ACL/并发并做两个普通用户与一个管理员的真实账号 E2E |
| M4 社区/通知/审核 | 已实现并本地验证 | 普通用户读写、互动、屏蔽、举报、通知、独立社区页面，以及管理员入案、不可变证据、内容/用户治理、作者通知、双审计与工作台已实现；148/148 本地测试通过 | 在真实 PG17 验证迁移/事务/权限，并用两个用户和一个管理员完成 staging E2E |
| M5 设计/文案 | 已实现并本地验证 | 统一 `--dufe-*` 色彩事实；资料/社区/后台接入同一纸墨朱金松针；首页移除致敬区；账号、资料、社区文案收紧；70 条等价规则和 1,262 条被后层必然覆盖的普通声明已安全裁剪并加入自动审计；无代码生产者的旧首页摄影/空教室容器级联已清除；公共 Masthead、响应式弹层/按钮和表单原语均已建立，账号、资料筛选与后台案卷已接入但保留各自构图 | 视觉问题随 M6 多宽度/无障碍验收修复；差异化历史规则只在有页面证据时调整 |
| M6 移动/无障碍/性能 | 本地实现完成，待 staging 指标 | 8 个核心页面桌面/手机 axe 16/16；五主视图在 320/360/375/390/414/768、667/844 横屏共 40/40；整页 520ms 入场已移除；首屏课程数据由 3.22MB 降至 633,576 字节，完整索引按需加载；课程选择器按课程预分组并以 40 门为窗口；首页无校园图片/字体请求；生产网关已验证 JS/CSS 水合、数据读取与源码拒绝，合并浏览器门禁 58/58 | 在 staging 实测 LCP/INP/CLS，并以真实登录态复核长会话和服务端列表游标 |
| M7 安全 | 进行中 | 初始威胁模型与问题清单完成；SEC-007、008、010、012、014、015 已修复或完成发布配置；主站、auth-api、小影生产依赖本地均为 0，相关失败关闭路径有回归 | 继续处理共享限流/外部协议；SEC-004 HTTPS 能力待人工核对上游，SEC-010/015 待 Linux staging 复核 |
| M8 全量测试 | 进行中 | 现有回归基线、8 个核心页面 Playwright + axe 门禁；原生 PG17 作业已加入邮箱/TOTP/恢复码单次并发消费、个人同步幂等/冲突/隔离、教师评价跨账号隔离/乐观并发、社区通知、管理员治理/审计 HTTP 流程 | 首次运行 CI，并补导入/审核、其余并发竞态与浏览器关键路径 |
| M9 部署准备 | 进行中 | 既有 Docker/Caddy/原子发布流程；原生 PG17 空库双迁移/ACL 作业与隔离恢复演练脚本已实现 | 首次运行 CI/恢复演练，完成 staging、镜像和回滚验收 |
| M10 PR/交付 | 未实现 | 独立功能分支已建立 | 全部里程碑后推送与建 PR |

## M0 基线证据

### 已验证

| 检查 | 结果 |
|---|---|
| ESLint：`app services tests scripts xiaoying-executor/bin xiaoying-executor/src` | 通过 |
| 课程/体验/基础设施/个人同步回归 | 31/31 通过 |
| auth-api 测试 | 10/10 通过 |
| 小影测试 | 26/26 通过 |
| 生产构建与 SSR | 构建成功，2/2 SSR 回归通过 |
| 主站、auth-api、小影生产依赖审计 | 均为 0 个已知漏洞 |
| 页面响应式检查 | 320/360/375/390/414/768/1280px × 5 个主视图，共 35 组无页面级横向溢出 |
| Git 工作区 | 已识别并保护用户未跟踪文件；开发分支独立于 `main` |

## M1 已验证切片：账号本地数据隔离

- 旧版 `dufesh:student-profile:v2` 只迁移到匿名作用域，不会自动进入任一登录账号。
- 匿名数据和每个 `user_id` 使用不同本地键；同步基线元数据也按用户分区。
- 退出或注销后清除当前账号的本地缓存与同步元数据，再恢复匿名作用域；不会显示或上传前一账号的数据。
- 登录后检测到匿名数据时只显示显式选择，“导入当前账号”前不会合并；“暂不导入”保留匿名数据。
- 本地 auth API 缺失或返回非成功状态时，账户中心进入匿名/离线状态，不再永久停在加载中。
- CI 已加入 auth-api 安装、生产依赖审计、10 项服务测试，以及个人存储/同步回归。
- 验证：账号存储/同步 9/9、体验与基础设施合计 22/22、auth-api 10/10、构建与 SSR 2/2；390px 浏览器页面无横向溢出。

## M1 已验证切片：用户名/邮箱/密码认证

- 新增增量迁移 `0006_credential_auth.sql`：用户唯一规范化用户名/邮箱、可选校园账号与验证时间、注册来源、最后登录时间、密码凭据和密码重置令牌。
- 密码使用真实 Argon2id 哈希；注册做 Unicode NFKC、字段白名单、长度、常见口令和身份派生口令检查，不保存明文。
- 登录对未知账号和错误密码返回同一结果；按 IP+规范化账号限流，服务端记录失败次数并在第 5、6、7 次分别进入 30 秒、2 分钟、15 分钟锁定。
- 注册和登录每次签发新会话；一次性重置令牌只存 HMAC 摘要、30 分钟过期、原子消费，重置成功撤销全部旧会话。开发模式可以把一次性码返回给本地测试；生产配置禁止该模式。
- “我的”页已有登录、创建账号、密码强度说明和重置状态；微信入口降为审核状态，不冒充可用。390×844 与 1440×900 浏览器检查均无页面级横向溢出，桌面首轮发现并修复了全站 footer 样式污染认证面板的问题。
- 验证：auth-api 16/16，包含真实 Argon2id、注册/冲突、统一失败、锁定、登录、单次重置与旧会话撤销；auth-api 生产依赖 high/critical 0；相关源码 ESLint 通过；主站构建与 SSR 2/2 通过。
- 未验证：当前机器无 Docker/PostgreSQL，`0006` 尚未在 PostgreSQL 17 空库、升级库和重复执行中运行；生产重置邮件缺发送服务凭据。本切片未部署。

## M1 已验证切片：账号公开资料底座

- `GET/PUT /api/auth/profile` 只允许当前有效会话读取和修改自己的资料；写入额外要求可信 Origin。
- 可修改字段严格限定为用户名、显示名和校园账号；`status`、角色、邮箱验证等字段无法通过请求体越权写入。
- 用户名做 NFKC 规范化和数据库唯一约束；校园账号更改后服务端清除原验证时间，界面明确显示其验证状态。
- 邮箱、注册时间、最后登录时间、账号状态、可选校园账号及验证状态已进入当前账号的会话/资料模型；“我的”页可编辑资料，不把未验证校园账号包装成真实身份。
- 验证：auth-api 18/18、相关源码 ESLint、主站生产构建通过；尚未实现邮箱/校园账号验证投递。

## M1 已验证切片：安全头像处理

- 新增增量迁移 `0007_user_avatars.sql`，头像与用户一对一并随账号级联删除；数据库只接受 512×512、512KB 以内的 WebP 及其 SHA-256 摘要。
- 上传只接受 JPEG、PNG、WebP，原图上限 5MB、最大边长 4096、最大 2000 万像素；服务端复核文件魔数和声明 MIME，拒绝动图/多页文件，纠正方向后裁切并重新编码为 WebP。
- 原始文件、EXIF 和定位信息不落库；头像写入和删除要求有效会话、可信 Origin，并按账号与 IP 限流。公开读取只返回活跃账号的头像，使用版本号、ETag、最多 5 分钟缓存与 `nosniff`，避免删除后长期保留旧副本。
- “我的 → 账号资料”已接入头像更换与删除，客户端提前提示格式和大小，但安全边界仍由服务端执行；隐私政策同步说明处理方式及公开展示属性。
- 验证：auth-api 21/21（含真实 Sharp 解码/重编码、伪造 MIME、随机无效内容、体积上限、读取缓存与删除闭环）；本机 `npm ci --ignore-scripts` 后原生模块仍可加载。真实 PostgreSQL 17 迁移和 Linux musl 镜像尚待 staging/CI 验证，本切片未部署。

## M1 已验证切片：邮箱验证令牌闭环

- 新增 `0009_email_verification.sql`：只保存一次性令牌的 HMAC 摘要、申请时的规范化邮箱、过期和消费时间；账号删除时级联清理，过期/已消费记录 30 天后由维护任务删除。
- 请求验证要求有效登录、可信 Origin、未验证邮箱及 IP+账号限流；新请求使该账号旧令牌失效。确认要求同一登录账号，令牌只能在账号仍活跃、邮箱未变化、未过期且未消费时原子成功一次。
- “我的 → 账号资料”已接入请求、一次性令牌确认、错误说明和账号状态刷新；生产不开放调试令牌回传，投递通道未配置时只显示“待开通”，不伪造邮件发送。
- 本地 `response` 模式覆盖了不可信 Origin、跨账号拒绝、旧令牌失效、成功确认、重复确认拒绝和会话状态更新；生产环境配置为 `response` 时启动失败。
- 验证：auth-api 32/32、相关 ESLint、基础设施/体验测试与生产构建通过。真实 PostgreSQL 17 迁移和生产邮件适配器仍未验证；校园账号验证未实现，本切片未部署。

## M1 已验证切片：管理员加密与 MFA 数据底座

- 新增增量迁移 `0008_admin_security.sql`：服务端角色、TOTP 因子、单次恢复码、绑定基础会话的短期提升会话和 append-only 管理审计事件。
- TOTP 按 RFC 6238 的 SHA-1、6 位、30 秒与 ±1 时间步验证；匹配后的时间步将由数据库条件更新原子消费，阻断并发重放。密钥使用 AES-256-GCM，加密 AAD 绑定因子 ID、用户 ID 与用途；key ID/keyring 支持逐步轮换。
- 恢复码由 128 位随机数生成，明文只在初始化时显示一次，数据库只存独立 `AUTH_ADMIN_RECOVERY_PEPPER` 的域分离 HMAC 摘要，不复用普通会话 pepper。
- 管理员默认关闭，只能用 `npm run admin:bootstrap -- <username-or-email>` 将明确的已有活跃账号初始化；不会自动提升首个注册者。重复初始化必须显式 `--rotate`，且会撤销该账号全部基础/提升会话并写系统审计。
- 验证：auth-api 26/26，包含 RFC 向量、密文不含明文、AAD 绑定、TOTP 时间窗/重放、恢复码规范化与 pepper 隔离、配置失败关闭；真实 PostgreSQL 迁移/原子并发、运行期数据库角色拆分、管理 API 与界面尚未完成，本切片未部署。

## M1 已验证切片：管理员 RBAC 与提升会话 API

- `/api/admin/session`、`/elevation`、`/overview`、`/users`、`/audit` 和用户状态写入均由服务端实时角色授权；匿名返回 401，普通用户在任何管理查询前返回统一 403，不根据对象是否存在产生差异。
- TOTP 或恢复码成功后签发独立 `__Host-`、HttpOnly、Secure、SameSite=Strict 提升 Cookie，固定最长 10 分钟且绑定当前基础会话；另一会话携带同一 Cookie 仍被拒绝。退出、密码重置、当前设备撤销、账号禁用与删除均清除或使提升失效。
- TOTP 时间步和恢复码在创建提升会话的同一事务中条件消费；相同 TOTP 时间步或恢复码只能成功一次。MFA 按 IP+用户+基础会话独立限流，Origin 缺失或不可信时不读取 MFA 数据。
- 用户列表只返回掩码邮箱和必要状态；账号禁用、全部会话撤销、提升撤销与包含脱敏前后状态/原因的审计事件同事务提交。写操作进入事务后再次验证管理员、基础会话与提升会话，避免授权检查后的竞态。
- 迁移器已支持创建独立 `AUTH_DB_USER` 非 owner 运行角色；运行角色可读写业务表，但不能更新/删除/截断 `admin_audit_events`，也不能访问 `schema_migrations`。Caddy 为 `/api/admin/*` 增加 no-store、noindex 同源代理。

## M1 已验证切片：管理员值守台

- 新增受保护的 `/admin`：未登录、普通用户、未配置 MFA、提升过期和连接失败均有独立状态，管理数据只在服务端确认管理员角色和有效短时提升后请求。
- TOTP 与恢复码使用同一明确输入，不在界面、日志或本地存储保留；值守台显示短时权限截止时间并支持主动结束提升会话。
- 概览、掩码账号列表、用户名/邮箱搜索和 append-only 审计时间线已接真实 `/api/admin/*`；停用/恢复必须填写至少 8 个字原因，并发送当前状态作为乐观锁，冲突后重新拉取。
- 停用确认明确说明全部既有会话会撤销；恢复确认明确说明旧会话不会复活。操作入口仅对会话角色为 admin 的用户显示，但直接访问仍由服务端 401/403/MFA 三层检查。
- 页面 metadata 与 robots 均禁止抓取；本地开发新增同源 auth/admin API 代理，可用 `AUTH_API_DEV_TARGET` 指向独立认证服务。源码 ESLint、基础设施/体验测试和生产构建通过。
- 浏览器视觉复核：桌面完整值守簿、390px 手机值守簿、390px MFA 入口及停用确认层均已检查；复核使用本机临时只读模拟响应，只证明渲染和交互状态，不替代真实管理员、Cookie 与 PostgreSQL E2E。
- 验证：auth-api 31/31，包含普通用户 403 且敏感 store 0 调用、严格 Cookie、跨会话拒绝、TOTP 重放、恢复码单次使用、恶意 Origin、独立限流、脱敏列表、禁用/会话撤销/审计闭环；全部 58 项仓库回归、ESLint、主站构建与 auth-api 生产依赖审计通过。真实 PostgreSQL 事务并发、角色权限与 shell 脚本因本机缺少环境尚未验证，真实账号浏览器 E2E 尚未完成，本切片未部署。

## M2 已验证切片：资料检索数据契约与只读 API

- 457 份已发布文件全部升级为带稳定 `material_id` 的资料实体；保留原预览/下载 URL，并从课程事实补齐课程 ID、教师、学院、学期、年级、类型和标签索引。当前 29 个资料目录均能匹配目录课程，没有按同名课程合并教学班。
- 新增公开只读 `GET /api/materials` 和 `GET /api/materials/:material_id`；列表支持资料标题、课程/课程号、教师、标签、类型、学期、年级及有界分页，响应只包含资料实体；未知或撤下 ID 明确返回 404，不回退到课程/选课流程。
- 检索查询做 NFKC 规范化、长度限制，分页每次最多 60 条；服务端搜索字段不会泄露进公开响应。静态元数据使用短期公开缓存，文件继续由既有 `/resources/files/*` 只读路径提供，旧 URL 不变。
- 独立 `/materials` 资料档案和 `/materials/:material_id` 详情已接入真实 API；课程库“搜索资料”与全站资料结果直接进入资料路由，不再打开教学班抽屉。结果行直接提供预览/下载，详情通过同源 HEAD 区分可用、404/410、403 与网络无法确认。
- 首次、加载、无结果、网络失败、资料不存在和文件不可用状态均有明确下一步；搜索词和课程、教师、类型、标签、学期、年级写入 URL。方向键可连续浏览结果，Enter 打开，Escape 清空并回到搜索框；reduced-motion 下取消位移与加载动画。
- 浏览器在 390×844 实际验证无横向溢出；“高数”得到 1 份资料，URL 同步，方向键聚焦并由 Enter 进入 `高数下.pdf` 详情；多结果连续方向键和 Escape、无结果状态均通过。桌面页视觉复核通过。
- 验证：资料领域 4/4、构建后 API/详情集成 1/1、全仓 107/107、auth-api 32/32、ESLint 和 vinext 生产构建全部通过。生产构建可识别资料列表 API、详情 API、资料页和动态详情页。本机未挂载正式 `/srv/resources`，真实文件抽样仍待 staging；本切片未部署。

## M0 审计结论

### 发布阻断

1. 退出账号后本地 `saved` 未按账号隔离；同浏览器 B 账号可能把 A 的本地数据合并上传。
2. 现有账号服务只有 disabled/mock OAuth，没有普通用户可用的真实注册登录。
3. 没有服务端 RBAC、管理员 MFA、管理 API 和管理员审计。
4. 资料清单没有进入真实搜索路径，点击课程会进入教学班选择。
5. 不存在 `teacher_id`、社区、通知、举报和审核数据模型。
6. TraceInt 的授权码与 Cookie 交换当前使用明文 HTTP，需要确认上游能力并消除或隔离链路风险。
7. 关键 UI 测试以源码正则为主，不能证明键盘、焦点、移动交互和权限真实可用。

### 前端与体验风险

- `DufeHubV2.tsx` 超过 5,000 行，页面、数据、弹层与路由耦合。
- 三层 CSS 含多个历史覆盖段；当前约 248KB 客户端 CSS，需在 M5 收敛。
- 首页课程事实已拆成 633,576 字节轻量核心与按需 3.22MB 完整索引；约 409KB 资料清单也已移出首屏并行请求。真实传输时延与 Web Vitals 仍待 staging 复核。
- 搜索文案承诺方向键和 Enter，但现有实现只有 Escape。
- 多个弹层缺完整焦点锁定；隐藏抽屉仍可能留在可访问树。
- 部分手机触控目标只有 34–40px；今日主 CTA 在 390×844 首屏以下。

### 教师工作簿审计

- 用户于 2026-08-09 提供正式完整版教师评价和教材工作簿；源文件未复制进仓库、未修改、未写数据库，文件摘要与无正文统计记录在 `docs/IMPORT_PREFLIGHT_2026-08-09.md`。
- dry-run 识别 863 行教师候选、3,321 条历史评价候选、10 组跨学院同名；1 行缺姓名、另 1 行缺来源键，两行涉及的 6 条评价拒绝自动导入。1,370 条评价含至少一项风险，全部历史评价无条件从私有 `pending` 开始。
- 教材计划含 1,143 行，教学班展开关系 2,660 行；13 门课程存在多个教材版本，777 行为“不指定教材”并带占位 ISBN，1 条课程号缺目录匹配、13 条教学班键缺直接匹配。新版模型按教学班保存多教材，不覆盖目录课程单一字段。

## M3 已验证切片：工作簿预检与教师/教材导入底座

- 新增 `scripts/academic-import-preflight.mjs` 与 `npm run import:academic:preflight`：读取指定教师/教材工作簿和当前课程数据，校验工作表、50MB 输入上限、稳定来源键、同名跨学院、评价隐私/攻击/时效风险、占位 ISBN、出版日期、课程与教学班匹配及多教材版本。
- dry-run 生成无评价正文的 `preflight-summary.json` 和逐行 `preflight-errors.csv`；CSV 防公式注入。真实运行结果共 7,987 条检查记录，3,556 accepted、4,422 warning、9 rejected；数据库写入为 0，源文件修改为 0。
- 新增 `0013_teacher_catalog_imports.sql`：导入批次、不可变行证据、append-only mutation、稳定 UUID 教师、独立来源教师键、别名、教师—教学班关系、可多项/版本化教材、私有历史评价候选和公开教师评价五维结构。
- 姓名与学院不设唯一约束；只有 `(source_system, external_teacher_key)` 唯一。历史候选只保存脱敏正文与原文摘要，管理员批准后才能生成“历史整理内容”，auth runtime 对批次、行证据、mutation 和候选完全撤权，对教师目录和教材事实撤销写权限。
- 验证：M3 与基础设施契约 27/27、全仓 Node 161/161、相关 ESLint、vinext 生产构建与 `git diff --check` 通过；嵌入式 PostgreSQL 17.5 已连续两遍执行 `0001`—`0013`，并验证同学院同名教师获得不同 UUID、重复来源键被拒绝、pending 历史候选不能公开。当前机器无原生 PostgreSQL/Docker，运行角色 ACL、并发连接、升级库数据场景仍待 staging；管理员审核 API 或教师页面尚未实现，本切片未部署。
- 生产依赖审计发现上游 `vinext@0.0.50` 固定的 `image-size@2.0.2` 有 2 个新披露 high 且暂无兼容修复；`fast-uri` 与 `nanoid` 已锁到修复版本。发布门禁保持失败，不伪造审计通过。

## M3 已验证切片：私有导入包、事务 apply 与依赖回滚

- dry-run 额外生成权限受限的 `private-import-bundle.json`；输出目录不得位于 `public/` 或 `app/`。包内历史评价只保存已脱敏正文与原文/规范化摘要，写入器会在数据库访问前再次拒绝未脱敏联系方式、超长正文、错误摘要和超量记录。
- 新增 `scripts/academic-import-apply.mjs` 与 `npm run import:academic:write`。每类源文件在事务内使用 advisory lock、文件 SHA-256 与映射版本防重复；第一次写入逐行证据和 append-only mutation，第二次相同 apply 返回原批次而不新增实体。
- 教师先创建独立 UUID，再登记唯一来源键；历史评价只进入 `pending` 候选。教材在教师映射后按教学班和 position 写入，完全相同教材跳过，变更教材创建新版本并把前版标为 `superseded`。
- 回滚先检查已公开候选和后续教材依赖；存在依赖时拒绝。按“教材批次 → 教师批次”回滚时只标记 `withdrawn/rolled_back/retired` 并追加反向 mutation，不硬删除证据。
- 迁移器新增 `IMPORT_DB_USER/IMPORT_DB_PASSWORD`，专用角色只获得导入表、教师目录、教材和候选所需的精确读写权限；不能读取账号、密码、会话、MFA、OAuth 或头像表。CLI 还要求 `IMPORT_ALLOW_APPLY=true`，避免误触写入。
- 本次真实规模私有包含 861 位可映射教师、3,294 条去重脱敏候选和 2,659 条教材关系。在嵌入式 PostgreSQL 17.5 首次 apply 全部成功，第二次完全幂等；随后两批按依赖顺序回滚，数量一一对应。私有临时包已删除，未写生产库、未部署。
- 验证门禁保持为 M3 与基础设施契约 27/27、全仓 Node 161/161、相关 ESLint、vinext 生产构建和 `git diff --check` 通过；生产依赖审计仍因已记录的 `image-size` 上游问题保留 2 个 high。

## M3 已验证切片：历史评价候选审核 API

- 新增 `0014_teacher_review_moderation.sql`：候选脱敏正文继续作为不可变证据；审核决定独立写入 append-only `teacher_review_candidate_decisions`，每个候选最多一个决定，并保留操作者显示快照、理由、批准正文和公开评价引用。
- `GET /api/admin/teacher-reviews/candidates` 提供最多 50 条的有界游标队列；`POST /api/admin/teacher-reviews/candidates/:id/decision` 只接受精确的 approve/reject 与 8—1000 字原因。两者都要求管理员短期 MFA 提升，写入还要求可信 Origin、JSON 和专项限流。
- 数据库函数在执行时再次锁定并校验管理员账号、基础会话、提升会话、来源批次和候选。批准会在同一事务创建“历史整理内容”公开评价、不可变决定和 `admin_audit_events`；拒绝不创建公开评价。重复决定返回 409，审计唯一键故障会把状态、决定和评价全部回滚。
- importer 不再拥有候选 UPDATE 权限，也不能写决定、公开评价或管理员审计；只可调用固定 `search_path` 的专用回滚函数，已批准或已有公开评价的候选不能被导入回滚。
- 验证：auth-api 68/68、基础设施契约 23/23、全仓 Node 164/164、嵌入式 PostgreSQL 17.5 真实 SQL 审核事务、相关 ESLint、vinext 生产构建通过。原生 PostgreSQL ACL、并发连接和浏览器管理员审核台仍待 staging；本切片未部署。

## M3 已验证切片：教师公开索引、详情与课表入口

- 新增同域公开只读 `GET /api/teachers`、`GET /api/teachers/:teacher_id` 和 `GET /api/teachers/:teacher_id/reviews`。姓名/学院过滤、UUID、每页数量和 base64url 游标均有边界校验；列表和评价采用稳定 keyset 分页并返回短时公共缓存头。
- 公开层只读取 `pending/active` 教师、`current/needs_review` 教学班与教材、`published` 评价。历史评价固定标注“历史整理内容”且五维评分为 `null`；未审核候选、来源摘要、导入行证据和内部身份状态不会返回浏览器。
- `/teachers` 用学院并列展示同名教师；详情按教学班列出课程和教材，五维均值只计算注册用户评价。课表卡片与课程教学班中的姓名进入带查询词的教师索引，直到课程数据带稳定 `teacher_id` 前都不按姓名猜详情。
- 视觉继续使用纸、墨、朱红的学术档案语言，并复用公共 Masthead；今日学习台未增加模块或摄影背景。Chrome 桌面和 390px 手机完成索引、详情和同名消歧复核，两页均无横向溢出。
- 验证：相关 API/数据库/体验/基础设施 41/41、全仓 Node 167/167、构建后渲染 4/4、CSS 审计、相关 ESLint、vinext 生产构建和 `git diff --check` 通过。浏览器使用临时只读响应只证明前端契约与布局，真实 PostgreSQL/Cookie E2E 仍待 staging；本切片未部署。

## M3 已验证切片：登录用户五维教师评价

- 新增 `0015_teacher_user_reviews.sql`：每个活动用户对同一教师最多一条未删除评价；数据库核验正文 SHA-256 与五项评分，历史整理评价正文、摘要和空评分保持不可变。
- 私有本人资源 `GET/PUT/DELETE /api/teachers/:teacher_id/my-review` 只从服务端会话取得作者，不接收评价 ID、作者 ID、状态或来源字段。写入要求可信 Origin、JSON、严格字段契约、用户与 IP 双限流和乐观版本；正文 NFKC 后 20—3000 字，五项评分均为 1—5 整数。
- 新建、编辑和软删除在事务中锁定活动账号、可见教师与本人评价；旧版本或重复新建返回当前版本冲突。账号注销触发评价撤下与去标识化，公开列表不再返回该正文，同时不阻断账号删除。
- 教师详情页接入登录提示、五维原生单选组、正文计数、本人评价编辑、冲突刷新和二次确认删除；隐藏评价可继续修改但不会因此自动恢复发布。视觉沿用纸、墨、朱红，不向今日学习台增加模块。
- 验证：auth-api 72/72、全仓 Node 172/172、构建后渲染 4/4、CSS 审计、相关 ESLint、vinext 生产构建与 `git diff --check` 通过；桌面与 390×844 手机完成表单布局及交互复核，无横向溢出。仓库级 `npx tsc --noEmit` 仍因既有社区/资料、Cloudflare Worker 类型和测试扩展导入错误失败，本次新增教师评价文件未出现在错误列表。真实 PostgreSQL/Cookie E2E 仍待 staging；本切片未部署。

## M4 已验证切片：社区、通知与治理数据库底座

- 新增增量迁移 `0010_community_foundation.sql`：主题、评论与一层回复、编辑历史、主题/评论点赞、收藏、用户屏蔽、提及和站内通知。
- 评论层级由数据库触发器限制为“根评论 + 一层回复”；主题和评论只允许软删除，硬删除由触发器与数据库权限双重拒绝，外键使用 `RESTRICT`/`SET NULL` 保留引用，通知额外保存 `fallback_path`，目标删除后可进入降级页。
- 点赞、收藏、提及和开放举报均使用唯一键或部分唯一索引防重复；通知按收件人和幂等键去重，并为未读游标建立部分索引。
- 举报队列、审核案件、案件—举报关系、用户制裁和治理动作已建模；治理动作与内容编辑历史使用 append-only 触发器，并从 auth runtime 撤销更新、删除和截断权限。
- 不可变历史只保留去标识化用户 UUID/显示快照，不使用会在账号注销时回写的外键；举报、案件与制裁证据不会因用户注销被级联删除，也不会反向阻断账号注销。回复的主题/父级/根级插入后不可重挂，案件—举报和审核动作必须与同一目标一致，通知降级路径只接受站内绝对路径。
- 验证：基础设施契约 19/19、全仓 Node 测试 109/109、`git diff --check` 通过。当前机器没有 PostgreSQL/Docker，真实 PostgreSQL 17 空库、升级、重复迁移、触发器和权限矩阵仍待 staging；尚无社区 API 或用户界面，本切片未部署。

## M4 已验证切片：社区公开读取 API

- 新增 `GET /api/community/topics`、`GET /api/community/topics/:id` 与 `GET /api/community/topics/:id/comments`，由同域 Caddy 和本地 Vite 代理进入 auth-api；匿名用户可读取公开内容，登录用户响应额外包含自己的点赞与收藏状态。
- 主题和根评论使用有界 keyset 游标分页，每页最多 30 条；游标做长度、字符集和结构校验，当前只开放真实实现的 `latest` 排序，不伪造热门算法。
- 双向屏蔽关系从列表和详情隔离；已删除主题返回 410 和本站 `/community` 降级路径，已删除或屏蔽评论保留线程位置但不返回正文和作者。
- 新增增量迁移 `0011_community_read_paths.sql`，为包含软删除墓碑的根评论与回复读取建立索引，不修改已提交的 `0010`。
- 验证：auth-api 39/39、基础设施契约 20/20、全仓 Node 测试 117/117、相关 ESLint 通过。真实 PostgreSQL 17 查询计划、迁移和浏览器页面尚未验证；写入、通知与审核 API 尚未实现，本切片未部署。

## M4 已验证切片：主题与评论写入 API

- 登录用户可发布、编辑和软删除自己的主题、根评论与一层回复；全部写入要求可信 Origin、有效站内会话，并同时消耗用户和 IP 两个独立专项限流键。
- 主题和评论按纯文本契约做 NFKC 规范化、长度/控制字符限制与额外字段拒绝；编辑和删除必须提交当前版本，过期写入返回当前版本冲突，不执行覆盖。
- 写事务先检查账号状态和有效社区制裁；非作者修改统一返回不存在。主题或回复目标不可用、与作者存在双向屏蔽时拒绝新评论。
- 对二层回复再次回复时，服务端将新回复挂到原根评论，并保留实际 `reply_to_user_id`；不会尝试向数据库写入第三层。编辑和删除在同一事务先保存不可变旧版本，再更新正文或软删除墓碑；冲突事务不会留下历史记录。
- 验证：auth-api 47/47、全仓 Node 测试 125/125、基础设施契约 20/20、相关 ESLint 与 `git diff --check` 通过。真实 PostgreSQL 17 的事务、约束和并发仍待 staging；互动、举报、通知、管理 API 与社区页面尚未实现，本切片未部署。

## M4 已验证切片：互动、屏蔽与举报 API

- 主题/评论点赞、主题收藏和用户屏蔽使用明确的幂等 `PUT`/`DELETE`；唯一约束继续作为最终防刷边界。新增互动前复核账号封禁、目标状态和双向屏蔽，取消互动不会借响应泄露不可见内容的计数。
- 拉黑禁止指向自己；事务同时写入屏蔽关系、移除双方在彼此内容上的遗留点赞、移除拉黑者对对方主题的收藏，并将对方发给拉黑者的既有通知设为隐藏。屏蔽关系继续由公开列表、详情、回复和互动查询共同执行。
- 举报只接受固定目标类型、UUID、原因码和有界纯文本说明；举报自己被拒绝。“其他”原因必须填写至少 8 字说明，重复的开放/审核中举报返回原记录，不改写原始原因和证据。
- 发布、互动和举报分别使用独立 token bucket，并都同时按账号和 IP 消耗键；举报桶为 5 次突发、每 5 分钟恢复 1 次。
- 验证：auth-api 53/53、全仓 Node 测试 131/131、基础设施契约 20/20、相关 ESLint 与 `git diff --check` 通过。真实 PostgreSQL 17 的唯一索引冲突、事务回滚和并发仍待 staging；通知、管理员审核与页面尚未完成，本切片未部署。

## M4 已验证切片：通知与两账号回复闭环

- 新建根评论时在同一事务向主题作者写 `topic_reply`；回复评论时向实际被回复用户写 `comment_reply`。自己回复自己、账号已失活和任一方向存在屏蔽时不生成通知。
- 主题和评论正文识别规范化 `@用户名`，单条最多 10 个不同账号；提及关系与通知唯一写入。回复收件人同时被提及时只保留回复通知，不制造重复提醒。
- 登录用户拥有通知游标列表、未读计数、单条已读、全部已读和隐藏接口；所有 SQL 均按 `recipient_user_id` 约束，通知列表额外过滤双向屏蔽。发件账号注销后不向浏览器返回去标识化 UUID。
- 主题或评论软删除会在同一事务把关联通知改成删除说明、清空正文并指向 `/community`，不保留失效深链或已删除内容预览。
- 两账号 API 测试真实启动 HTTP 服务并跑通：B 登录回复 A 的主题，A 的未读数从 0 变 1、列表出现 `topic_reply`，A 单条已读后未读数归零。store 测试分别覆盖回复通知 SQL、提及规范化/去重、收件人作用域、屏蔽过滤和软删除降级。
- 验证：auth-api 58/58、全仓 Node 测试 136/136、基础设施契约 20/20、相关 ESLint 与 `git diff --check` 通过。真实 PostgreSQL 17 事务和浏览器界面仍待 staging；管理员内容处理通知、公告与社区页面尚未完成，本切片未部署。

## M4 已验证切片：管理员举报与治理闭环

- 短期 MFA 提升管理员可读取开放/审核中举报、把举报纳入案件，并执行内容隐藏、恢复、软删除，用户警告、限时停发、封禁、解封和驳回。原因做 NFKC、8—1000 字与控制字符检查，动作字段严格白名单；内容/用户动作不混用，状态冲突返回 409，管理员不能制裁自己。
- 打开案件和执行动作均在事务中重新校验管理员角色、基础会话与提升会话；提升被撤销时在读取举报或目标之前回滚。跨站请求、普通用户和额外字段均不能进入敏感 store。
- 案件动作同时写社区 append-only 治理记录和全局 append-only 管理员审计。内容状态、关联举报结案、用户制裁、旧通知脱敏与作者处理结果通知和两份审计同事务提交；内容只做软删除。
- 验证：auth-api 63/63、全仓 Node 测试 141/141、基础设施契约 20/20、相关 ESLint 与 `git diff --check` 通过。真实 PostgreSQL 17 的部分唯一索引、触发器、事务回滚、并发与 runtime 权限仍待 staging；社区页面和管理员举报工作台尚未接入，本切片未部署。

## M4 已验证切片：社区页面、通知中心与审核工作台

- 新增独立 `/community` 和 `/community/topics/:topic_id`：最新主题游标列表、发布、详情、根评论/一层回复、编辑冲突、软删除、点赞、收藏、屏蔽与撤销、举报、通知列表/未读/单条与全部已读/隐藏均直接调用真实 API。社区不进入五项主导航，只从“我的”进入。
- 前端只有已经实现的时间排序，不伪造热门榜或虚假计数；加载、空、网络失败、内容不可用和登录状态不确定均给出明确下一步。通知深链只接受本站路径，弹层/抽屉具备滚动锁、焦点陷阱、Escape 关闭和焦点恢复。
- `/admin` 在管理员完成短期 MFA 后加载举报队列；可查看举报时刻的不可变证据、入案并只执行服务端返回的允许动作。隐藏内容只能恢复或删除，生效制裁中的账号只能解封，且服务端事务再次校验同一矩阵；刷新失败不会把已成功的高权限写操作误报为失败。原因与期限在前后端共同校验，动作完成后刷新队列和 append-only 审计。
- 新增 `0012_community_report_evidence.sql`，举报事务保存不可变证据快照；旧举报回填后由触发器拒绝修改快照。审核队列同时返回目标当前状态、有效制裁与状态安全动作，避免界面提供必然 409 或无法恢复的操作。
- 浏览器在 1280 桌面和 390×844 手机验证；手机无横向溢出。后端断开时真实显示重试错误；临时内存响应只用于走通列表、点赞、详情、回复、通知、举报长度、滚动锁和焦点恢复，不作为数据库证据。
- 验证：auth-api 67/67、基础设施契约 21/21、全仓 Node 测试 148/148、构建后渲染 4/4、相关 ESLint 与 `git diff --check` 通过。真实 PostgreSQL 17、Linux 容器静态资源、真实 Cookie/管理员和并发 E2E 仍待 staging；本切片未部署。

## M5 已验证切片：统一色彩事实、内容归位与用户文案

- 建立全局 `--dufe-*` 语义色彩事实，`--journal-*`、`--ds-*`、资料、社区和后台局部变量改为兼容别名；修复“我的”社区入口引用社区模块私有变量而丢失朱线的问题。
- 新建个人日程默认朱红；旧 `blue` 数据键继续兼容，但显示为“炭墨”并实际渲染炭墨色。旧版今日和空教室兼容样式中的明确蓝色值也替换为当前纸墨朱色。
- Sci-Hub/Alexandra Elbakyan 致敬区从今日学习台移到“我的”校园影集之后；创作者“会一直有人管”“2024—∞”改为可核实的课余共创与“始于 2024”。
- 资料页标题改为具体搜索能力，进入手机页面不再自动聚焦并弹出键盘；账号不可用状态不再暴露“生产配置/本地开发/资质审核”等内部措辞；社区举报、屏蔽与编辑反馈改为用户能直接理解的结果。
- 社区/后台弹层增加 `100dvh`、安全区、内部滚动和 `overscroll-behavior`；社区手机操作与日程色板补齐 44px 触控目标，账号动画补全减少动态偏好。
- 验证：三角色只读审查完成；全仓 Node 150/150、构建后渲染 4/4、相关 ESLint 与 `git diff --check` 通过。浏览器验证桌面“我的”内容顺序和共享 Token、手机资料页不抢焦点且无横向溢出、日程默认朱红并将旧蓝键显示为炭墨。本切片未部署；历史三层全局 CSS 仍待分批裁剪。

## M5 已验证切片：全局 CSS 等价去重

- 新增 `scripts/audit-css-cascade.mjs` 与 `npm run audit:css`：解析三个全局样式表的媒体查询、支持查询、关键帧和嵌套祖先上下文，只把“同上下文、同选择器、同完整声明”的规则视为等价副本；修复模式只删除较早副本并保留最后生效位置。
- `globals.css`、`product-system.css`、`red-access-system.css` 共移除 70 条早期等价副本，减少 401 行、6,724 字节源文件；没有把声明不同的 V14—V18 覆盖混作重复项。新增 Node 回归阻止相同级联债务重新进入。
- 验证：CSS 审计通过；全仓 Node 151/151、构建后渲染 4/4、相关 ESLint 与 `git diff --check` 通过。浏览器在 1280px 桌面与 390×844 手机逐一打开今日、找课、课表、空教室和我的，全部无横向溢出；手机资料页仍不抢焦点，社区页沿用统一朱红 Token，两页均无横向溢出，控制台无页面错误。本切片未部署。

## M5 已验证切片：逐属性清除必然失效的历史覆盖

- CSS 审计扩展为按三个全局样式表的真实导入顺序检查：只有祖先媒体/支持/关键帧上下文完全相同、选择器文本完全相同、属性名完全相同，且后层声明按 `!important` 规则必然获胜时，才删除较早的普通声明。
- 自定义属性一律排除，保证顶部 `--ds-* → --dufe-*` 等设计系统语义事实不会因为计算结果等价而丢失；不同选择器、不同断点、简写/长写属性和权重无法静态证明的情况全部保留。对应合成用例覆盖同上下文覆盖、不同媒体查询、简写/长写和 `!important` 边界。
- 本切片移除 1,262 条必然失效的普通声明，三个源文件减少 40,485 字节；主构建 CSS 从上一切片的 272,024 字节降至 243,817 字节。验证：CSS 审计、全仓 Node 153/153、构建后渲染 4/4、相关 ESLint 与 `git diff --check` 通过；1280px 与 390×844 再次逐一复核五个主视图，资料/社区页与 Token 计算值，无横向溢出、抢焦点或浏览器页面错误。本切片未部署。

## M5 已验证切片：公共工作区 Masthead

- 新建 `PublicMasthead`，把资料检索、资料详情和校园回廊重复的品牌字标、导航、活动态与通知动作收敛为一个组件；管理员值守台仍使用独立顶栏，避免把公共浏览语境误套到高权限工作区。
- 公共顶栏统一使用纸墨朱 Token、78px/68px 桌面与手机节奏及 44px 操作目标；手机只保留当前任务和必要登录/通知入口，次要跨区链接仍保留在 DOM 中并按明确规则收纳。资料与社区模块原有重复 `.siteHeader`、`.wordmark` 规则已删除。
- 新增结构回归，锁定 `aria-current`、导航标签、手机显隐契约、44px 操作目标及三个页面的共享组件接入。验证：全仓 Node 154/154、构建后渲染 4/4、CSS 审计、相关 ESLint 与 `git diff --check` 通过；浏览器在 1280px 与 390×844 复核资料检索、资料详情、社区及五个主视图，均无横向溢出或自动抢焦点，页面控制台无错误。未启动 auth-api 时社区显示既有离线重试状态，不作为真实账号/数据库 E2E 证据。本切片未部署。

## M5 已验证切片：社区与后台弹层及操作按钮

- 新建 `DialogBackdrop` 与 `DialogActions`，统一社区举报、回复编辑、管理员账号处置和举报案卷的遮罩背景、层级、安全区、手机底部贴合、点击空白关闭、忙碌状态防误关，以及 44px 主次操作按钮和禁用态；高优先级审核案卷仍使用更高层级，不与普通弹层混淆。
- 管理员账号处置补接 `useModalFocus`，获得与社区弹层一致的焦点陷阱、Escape 关闭、背景滚动锁和关闭后焦点恢复；原先分散在社区/后台 CSS Module 的三份遮罩规则已删除。
- 新增结构回归锁定四处接入、遮罩点击边界、忙碌状态、安全区、共享按钮及管理员焦点引用。验证：全仓 Node 155/155、构建后渲染 4/4、CSS 审计、相关 ESLint 与 `git diff --check` 通过；按钮合并后再次在 1280px 与 390×844 复核五个主视图、社区和管理员入口，无横向溢出或页面控制台错误。当前未启动 auth-api，登录后弹层真实交互仍需随 PostgreSQL/Cookie staging E2E 验证。本切片未部署。

## M5 已验证切片：编辑型表单原语

- 新建 `FormField` 与独立 CSS Module，把编辑场景重复的标签、字数计数、正文提示、纸面下划线、朱红 `:focus-visible`、44px 最小控件高度和减少动态偏好收敛为一份实现；标题可使用展示字体变体，页面版式仍由各自模块控制。
- 社区主题发布、主题编辑、举报类型/说明、回复编辑及教师评价正文已接入共享原语；删除社区 24 行和教师 15 行重复控件规则。五维评分继续使用语义化 `fieldset` 与原生 radio，不为追求“统一”改成通用矩形控件。
- 新增结构回归锁定四类页面接入、计数、展示字体、44px、键盘焦点、朱红 Token 和减少动态偏好。验证：全仓 Node 173/173、相关体验测试 16/16、构建后渲染 4/4、CSS 审计、相关 ESLint、vinext 生产构建与 `git diff --check` 通过。仓库级 `npx tsc --noEmit` 仍只报既有社区/资料、Cloudflare Worker 环境类型和测试扩展导入错误，共享表单与教师评价未新增类型错误。
- 浏览器使用临时本地响应复核社区发布区和教师评价表单的前端契约与布局：桌面和 390px 级手机宽度均无横向溢出，教师正文字数随输入更新；临时响应不作为 Cookie 或数据库证据，已清理。本切片未部署。

## M8 已验证切片：仓库 TypeScript 门禁

- 修复社区举报异步提交对可空目标的闭包收窄；为资料详情建立明确实体边界，消除来自 `.mjs` 目录模块的隐式 `any`，没有把数据改成宽泛 `any`。
- `tsconfig` 的 `.ts` 测试导入与既有 `noEmit` 模式通过 `allowImportingTsExtensions` 对齐；加入与 Wrangler 4.92 兼容的官方 `@cloudflare/workers-types`，并声明当前部署可选的 D1 `DB` 绑定。失败的 latest v5 安装没有写入依赖文件，最终锁定兼容 v4。
- 新增 `npm run typecheck` 并加入 GitHub quality workflow；基础设施回归锁定命令、CI 步骤、官方类型和 D1 绑定声明，防止以后通过删除检查来“修复”类型错误。
- 验证：`npm run typecheck`、全仓 Node 174/174、构建后渲染 4/4、CSS 审计、相关 ESLint 与 `git diff --check` 通过。主站生产依赖审计仍因 `vinext@0.0.50 → image-size@2.0.2` 保留 2 个 high，没有使用破坏性 `npm audit fix --force` 降级 vinext；本切片未部署。

## M5 已验证切片：资料筛选与后台安全案卷字段

- 资料页课程、教师、类型、学期和年级筛选接入共享 `FormField`；档案红脊、索引编号、桌面侧栏和手机 1+2+2 排列保持不变，只删除重复标签、选择框和焦点规则。
- 管理员账号处置原因、举报入案原因、治理动作、制裁时长和处置原因接入同一字段语义，并为长文本加入实时字数；后台继续消费自身纸墨与值守红 Token，TOTP 等安全提权输入保留专用交互，不为形式统一扩大共享组件职责。
- 验证：主仓 Node 91/91、个人存储/同步 9/9、auth-api 72/72、构建后渲染 4/4、`npm run typecheck`、CSS 审计、相关 ESLint 与 `git diff --check` 通过。浏览器在桌面与 390px 手机复核资料页，手机 `scrollWidth` 375、无横向溢出；资料类型切换为 PDF 后结果由 457 份更新为 151 份并出现“全部清空”。管理员未登录入口可正常渲染；登录后真实 Cookie/数据库案卷 E2E 仍保留为 staging 门禁。本切片未部署。

## M5 已验证切片：账号凭据与资料字段

- 登录、注册、密码找回、账号资料编辑和一次性邮箱令牌接入共享 `FormField`，统一标签/提示、44px 触控高度、纸面下划线、朱红键盘焦点和减少动态偏好；用户名、邮箱、当前/新密码的 `autocomplete`、类型、长度和必填约束均保持原样。
- 账号页继续以“账户 / ACCOUNT”黑线关口和本机数据红脊作为识别元素；头像文件选择、不可逆注销确认、账号提交按钮和邮箱验证动作保留专用语义，没有为了组件统一削弱安全层级。
- 验证：主仓 Node 91/91、个人存储/同步 9/9、auth-api 72/72、构建后渲染 4/4、`npm run typecheck`、CSS 审计、相关 ESLint 与 `git diff --check` 通过。浏览器用临时本地只读会话分别复核匿名登录和登录态资料编辑：390px 下字段高度约 45px、页面 `scrollWidth` 375，无横向溢出；临时响应未写数据库且已清理。本切片未部署。

## M5 已验证切片：清除退役视觉容器

- 全仓代码与生成脚本均已不再生产旧首页摄影容器 `.campus-window` 和旧空教室容器 `.room-stack`；从 `product-system.css` 与 `red-access-system.css` 删除其桌面、手机、阴影、圆角和配色等 139 行历史级联，不触碰当前今日学习台、校园影集或空教室组件。
- 新增产品体验回归，锁定组件与三层全局 CSS 不得重新引入这两个退役类。删除组合选择器后暴露的一条 `.campus-gateway` 同属性完全遮蔽声明也由 CSS 审计器精确移除。
- 验证：主仓 Node 92/92、个人存储/同步 9/9、auth-api 72/72、构建后渲染 4/4、`npm run typecheck`、CSS 审计、相关 ESLint 与 `git diff --check` 通过。浏览器在 1280px 桌面与 390×844 手机复核今日学习台和“我的”：桌面 `scrollWidth` 1265、手机 `scrollWidth` 375，均无横向溢出；首页仍为今日学习任务，“我的”仍加载校园影集。本切片未部署。

## M6 已验证切片：非首屏资料清单与键盘焦点

- 首页初始化只请求 3,218,571 字节课程事实；408,712 字节资料清单移入 `HubApp`，课程工作台渲染后才通过 `requestIdleCallback` 空闲预取，不支持该 API 的 WebView 使用 1.2 秒回退。用户先打开全站搜索或课程抽屉会直接触发同一个单次请求，不会重复下载。
- 资料加载状态区分空闲、加载、完成和失败；失败时不把空数组显示成真实“0 份资料”，课程抽屉给出关闭后重试的具体下一步。管理员提权、社区回复和资料搜索输入从通用 `:focus` 改为 `:focus-visible`，保留可见边线/阴影但不让鼠标和触屏残留键盘焦点样式。
- 新增体验回归锁定首屏不直接读取资料清单、空闲 API 与旧 WebView 回退、搜索/课程按需触发、失败说明和三处键盘焦点选择器。验证：主仓 Node 93/93、个人存储/同步 9/9、auth-api 72/72、构建后渲染 4/4、`npm run typecheck`、CSS 审计、相关 ESLint、生产构建与 `git diff --check` 通过。
- 浏览器最终时序中课程数据请求于约 1.44 秒结束，资料清单约 2.58 秒才开始/完成；320×844、390×844、667×375 横屏分别走完五项主导航，页面 `scrollWidth` 均小于 `innerWidth`。资料搜索经键盘获得 `:focus-visible`，本切片未部署。

## M6 已验证切片：桌面与手机 axe 门禁

- 新增 Playwright + axe 浏览器套件，覆盖今日学习台、资料、社区、教师、隐私、用户协议、账号注销和管理员入口；桌面 Chrome 与 Pixel 5 两个项目共 16 个场景，检查 WCAG 2/2.1 A/AA，并只以真实 `critical`、`serious` 违规阻断，不排除页面区域或关闭规则。
- 本地复用已安装 Chrome，CI 安装标准 Chromium；质量工作流增加浏览器安装和 `npm run test:a11y`，超时预算从 20 分钟调整为 30 分钟。基础设施契约锁定脚本、双项目、axe 与 CI 步骤，测试报告和 trace 目录不进入版本控制。
- 修复手机端全站搜索按钮的可读名称；把纸面静默文字、深色页脚、社区规则和 onboarding 活动态调整到 AA 对比度，并让法律正文邮件链接使用下划线区分。色阶仍沿用纸、墨、朱砂体系，没有切回蓝色或通用组件色。
- 资料检索不再把包含预览和下载链接的整张卡片错误声明为 listbox 选项；输入改为原生搜索框，结果改为语义列表/列表项，既有方向键聚焦、Enter 打开与 Escape 清空逻辑保持不变。
- 验证：axe 16/16、主仓 Node 61/61、个人存储/同步 9/9、auth-api 72/72、小影 26/26、构建后渲染 4/4、`npm run typecheck`、CSS 审计、全仓 ESLint、生产构建与 `git diff --check` 通过。主站生产依赖审计仍因 `vinext@0.0.50 → image-size@2.0.2` 保留 2 个 high；auth-api 与小影均为 0。本轮未部署。
- `npm run lint` 现在明确排除 Git 已忽略的 `.codex_tmp/`、`outputs/` 与 `work/` 生成产物，避免历史构建副本污染源码结果；这些用户未纳管目录没有被修改或删除。

## M6 已验证切片：平板/横屏门禁与性能预算

- 新增 768×1024 平板和 667×375 手机横屏 Playwright 项目，逐一打开今日学习台、课程库、课表、空教室和“我的”五个主视图；测试等待真实课程数据完成，确认目标视图可见、没有未捕获脚本错误，并以根节点和 `body` 的实际 `scrollWidth` 阻断页面级横向溢出。10/10 场景通过。
- CI 从只执行 axe 套件改为执行完整 `npm run test:browser`，一次覆盖桌面/手机 axe 16 个场景和平板/横屏布局 10 个场景；基础设施契约锁定四个浏览器项目、五个主视图和溢出断言，防止脚本存在但 CI 漏跑。合并门禁 26/26 通过。
- 根目录新增 `performance-budgets.json` 作为机器可读的阶段预算：当前 3,218,571 字节课程事实不得回涨到 3,250,000 字节以上，408,712 字节资料清单不得回涨到 420,000 字节以上；下一阶段首屏课程数据必须拆到不高于 750,000 字节。staging 验收线明确为 LCP≤2.5 秒、INP≤200 毫秒、CLS≤0.1；本地文件体积检查不冒充真实 Web Vitals。
- 验证：主仓领域 Node 63/63、个人存储/同步 9/9、auth-api 72/72、小影 26/26、构建后渲染 4/4、浏览器门禁 26/26、`npm run typecheck`、CSS 审计、全仓 ESLint、生产构建与 `git diff --check` 通过。当前认证/社区/教师 API 未随浏览器门禁启动，页面按既有离线契约降级；这不是登录态数据库 E2E 证据。本切片未部署。

## M6 已验证切片：首屏轻量课程核心与按需完整索引

- 课程生成链现在从同一完整事实同步产出 `course-core.json`：用课程、教师、时间文本、场馆和教室字典压缩重复值，保留全部 5,870 个时段的稳定 ID、学期、课程、教师、星期、节次、有效周、时间文本和地点。逐条与 `course-data.json` 比对通过，不改变 Course—Section—Meeting 事实或既有本机课表 ID。
- 首页初始课程请求从 3,218,571 字节降为 633,576 字节，达到不高于 750,000 字节的预算。首页、未来七天、作业课程名与当前时段空教室使用轻量核心；课程库、课表、空教室、班级设置、全站课程搜索和课程详情才加载完整索引。完整请求单例去重，失败时保留今日学习台并给出重试；课程详情在完整数据到达前不展示不完整教学班。
- `performance-budgets.json` 已把轻量核心设为真实首屏预算，并分别约束 3.22MB 按需课程索引和 409KB 空闲/按需资料清单。生成器提供 `npm run build:course-core`，CI 逐条验证轻量时段与完整事实一致，并继续锁定 LCP≤2.5 秒、INP≤200 毫秒、CLS≤0.1 的 staging 验收线。
- 两个浏览器视口真实验证：首页稳定后已请求 `course-core.json` 且未请求 `course-data.json`；点击主导航“我的课表”后完整索引才出现，随后真实课表页渲染。布局 10/10、请求时序 2/2，与 axe 合并后浏览器门禁 28/28 通过。主仓领域 Node 64/64、个人存储/同步 9/9、auth-api 72/72、小影 26/26、构建后渲染 4/4、生产构建、TypeScript、CSS 审计与全仓 ESLint 均通过；本机 API 离线降级不作为登录态数据库 E2E 证据。本切片未部署。

## M6 已验证切片：全目标视口与生产网关

- 响应式门禁扩展到 320×844、360×800、375×812、390×844、414×896、768×1024、667×375 和 844×390；每个视口逐一打开今日学习台、课程库、课表、空教室和“我的”，检查目标视图、未捕获脚本错误以及根节点/body 实际宽度，共 40/40 通过。
- 删除 `.page-wrap` 的整页 520ms `journal-reveal`，页面不再先隐藏再整体入场；局部状态反馈与 `prefers-reduced-motion` 契约保留。axe 稳定等待改为两个动画帧，响应式检查等待字体就绪，不以固定空等掩盖加载问题。
- 浏览器门禁改为先构建、再通过真实生产启动链运行。首次运行真实发现 `vinext` 未提供 `dist/client` 中的 public 数据和哈希 JS/CSS，页面只能 SSR 但无法水合；现由受限网关白名单提供 `/assets/`、`/data/`、`/images/`、`/.well-known/` 与明确根文件，vinext 只绑定回环端口，其余请求继续代理。独立进程测试验证主页、JS/CSS、轻量/完整课程数据、HEAD、缓存、缺失 404 和源码拒绝。
- 最终生产浏览器门禁为 axe 16/16、响应式 40/40、首页按需完整索引 1/1，合计 57/57。主仓本切片 54/54、个人存储/同步 9/9、auth-api 72/72、小影 26/26、构建后渲染 4/4、生产构建、TypeScript、CSS 审计、全仓 ESLint 与 `git diff --check` 均通过；真实登录态和 PostgreSQL 数据库 E2E 仍待 staging，本切片未部署。

## M6 已验证切片：有界课程选择器与图片/字体请求

- 课程选择器把当前学期 5,870 条时段先按课程建立一次性索引，不再为最多 80 张已挂载卡片各自全表扫描。首次只渲染 40 门，每次明确追加 40 门；搜索模式、关键词、院系、专业、年级、星期和节次变化都会回到首批，避免用户筛选后继续保留旧的扩展窗口。
- 生产浏览器真实打开手机课程选择抽屉，验证 40 → 80 的追加和筛选后的 3 → 40 重置。测试同时发现并修复了固定底部导航拦截抽屉底部按钮的问题：抽屉打开时主导航退出可见与命中层，关闭后恢复。
- 现有资料、教师、评价、社区、评论和通知均已有 20—36 条服务端分页/游标边界，不额外引入会破坏可访问性与交互语义的 DOM 虚拟化。校园照片只在“我的”影集和创作者弹层按需渲染并延迟解码；今日学习台生产请求中没有 `dufe-*` 校园照片，也没有字体资源请求，全站继续使用系统字体栈。
- 验证：axe 16/16、响应式 40/40、性能请求与结果窗口 2/2，生产浏览器门禁 58/58；主仓本切片 55/55、领域导入/教材/教师评价 12/12、个人存储/同步 9/9、小影 26/26、渲染/CSS 7/7、生产构建、TypeScript、CSS 审计、全仓 ESLint 与 `git diff --check` 均通过。本地结果不冒充 staging Web Vitals，本切片未部署。

## M7 已验证切片：小影公共错误边界

- 新增小影公共错误映射模块，把 HTTP 状态、配对错误、白果云/图书馆供应方错误和任务错误转换为稳定错误码与固定用户文案。HTTP 总异常处理和任务执行器都不再返回原始 `error.message`；未知任务适配器异常只返回 `TASK_FAILED`，审计仍只保存最小错误码。
- JSON 解析失败在请求边界转换为 400 `INVALID_REQUEST`，正文片段不会进入响应；内容类型和体积分别使用 415/413。邀请码等既有业务拒绝继续保持 400 契约，供应方过期、超时、协议变化和不受信任跳转只从白名单文案映射，不拼接第三方响应。
- 回归以含 `private-provider-token` 的畸形 JSON 和含 `private-session-cookie` 的模拟上游异常验证 HTTP 响应、任务结果、审计记录均不含原文。小影 27/27、安全/生产网关契约 28/28、主仓关联回归 27/27、生产构建、TypeScript、CSS 审计、全仓 ESLint 与 `git diff --check` 通过；小影生产依赖 high/critical 为 0。主站已知 `vinext → image-size` 2 个 high 仍阻断发布，本切片未部署。

## M7 已验证切片：小影任务租约与持久幂等

- 空位检查、定时预约和预约守护新增 SQLite 租约令牌与到期时间；claim 原子成功后才允许访问适配器，finish 必须携带相同令牌。旧工作进程即使晚返回，也不能覆盖已经恢复或取消的任务状态。
- 只读空位检查在租约过期后可安全重领；定时预约和预约守护可能已在第三方生效，因此过期后分别进入 `review_required` 和 `paused`，以去重通知提示用户核对当前预约，绝不自动重放不确定的 `reserve/cancel`。小影页面保留需核对的定时任务，不让它静默消失。
- 预约/取消确认新增持久化执行账本：幂等键只保存 SHA-256 摘要并绑定任务类型/载荷摘要，运行中返回冲突，完成结果可跨 SQLite 关闭/重开原样回放且只审计一次，同键不同内容被拒绝。执行中断且租约已过期时转为 `EXECUTION_REVIEW_REQUIRED`；账本保留 30 天并在后续 claim 时清理。预览继续是短期内存对象，重启后只能重新预览，不会绕过确认或自动写入。
- 验证：小影 31/31、安全/生产网关契约 28/28、主仓关联回归 27/27、个人存储/同步 9/9、生产构建、TypeScript、CSS 审计、全仓 ESLint 与 `git diff --check` 通过；小影生产依赖 high/critical 为 0。本切片未部署。

## M7 已验证切片：小影邀请码有界配额

- 正式环境除至少 12 位随机邀请码外，必须显式设置 `XIAOYING_DEFAULT_INVITE_MAX_USES=1..500`；缺失、非整数或越界时进程在打开服务前拒绝启动。仓库新增无真实值的 `xiaoying.env.example`，README 和部署检查表同步说明服务器环境文件须为 600 权限。
- 默认邀请码不再以 `max_uses=NULL` 创建；已有数据库启动时会把同一邀请码升级为当前有界配额。最终消费在 `BEGIN IMMEDIATE` 事务内执行带 `max_uses IS NOT NULL AND use_count < max_uses` 的条件更新，前置哈希匹配即使读取到旧计数也不能超发。配置新邀请码会原子替换哈希、重置新批次计数并让旧码立即失效。
- 验证：生产缺配置拒启、达到上限、重启后仍拒绝、旧码失效、新码新配额均通过；小影 33/33，相关 ESLint 通过。完整关联构建/安全门禁在本切片提交前再次执行，本轮未部署。

## M7 已验证切片：服务级 CSP nonce 与内容哈希

- 主站生产网关为每个 vinext 文档请求生成独立 18 字节随机 nonce，把 CSP 同时传入 vinext 请求和最终浏览器响应；vinext 因而为 RSC 引导、模块入口等全部可执行内联脚本写入匹配 nonce。`script-src` 不再允许 `unsafe-inline`，第二次请求必须得到不同 nonce；JSON-LD 保持不可执行结构化数据。
- 主站外置样式块只允许同源加载，React/dnd-kit 运行时必须写入的 transform 等样式属性单独限制在 `style-src-attr 'unsafe-inline'`，该例外不授予内联脚本或 `<style>` 执行权。网关直接提供的 JS、CSS、数据与图片响应使用 `default-src 'none'; sandbox`。Caddy 删除会覆盖服务策略的全局宽松 CSP，其他安全头保持不变。
- 小影页面在服务启动时从实际替换完子路径的 HTML 计算脚本、样式块和样式属性 SHA-256；策略只允许这些精确哈希，且样式属性使用 `unsafe-hashes`，不含 `unsafe-inline`。页面内容变化但未进入哈希时会被浏览器失败关闭。
- 验证：生产浏览器 58/58、主仓 67/67、个人存储/同步 9/9、auth-api 72/72、小影 33/33、构建后渲染 4/4、TypeScript、CSS 审计、全仓 ESLint、生产构建与 `git diff --check` 通过。auth-api 与小影生产审计均为 0；主站 `vinext → image-size` 的 2 个 high 仍阻断发布。本切片未部署。

## M7 已验证切片：正式域名单一 HTTPS 入口

- Caddy 的直接 IP 明文站点不再导入应用、认证、社区、资料或小影代理，只执行 `308 https://dufesh.cn{uri}`；路径与查询参数保留，浏览器不会在 IP/HTTP 来源下进入页面或收到会话相关响应。`www.dufesh.cn` 继续使用同样的主域名规范化规则。
- 基础设施契约精确锁定 IP 站点块只能包含该 308 规则，避免以后把公共代理宏重新接入。验证：安全/生产网关契约 28/28、`git diff --check` 通过；本机没有可用 Caddy/Docker，真实监听、跳转链与 TLS 仍须 staging/生产 `curl -I` 复核。本切片未部署。

## M7 已验证切片：隔离 vinext 漏洞图片解析器

- 2026-08-09 核对 GitHub Advisory Database 与 npm 最新元数据：GHSA-w3rx-r6r6-pgpr、GHSA-5p2g-fcmc-qvqq 均覆盖 `image-size<=2.0.2`，官方截至 2026-08-07 仍列为无修复版本；最新 vinext 也继续依赖 2.0.2，强制 `npm audit fix` 只会降级框架而不提供可信修复。
- 根依赖新增项目内 `image-size@2.0.3-dufesh.0` 并通过 npm `$image-size` override 覆盖 vinext；该包不复制或调用任何图片解析器，所有 Buffer/文件尺寸探测立即以固定错误失败关闭。全新目录 `npm ci` 后 vinext 实际去重到该包，原 2.0.2 不在依赖树，主站生产审计从 2 个 high 降为 0。
- 基础设施门禁同时禁止 `app/` 中的本地 PNG/JPEG/GIF/WebP/AVIF/SVG/ICO/BMP/TIFF import 与图片 metadata 文件。现有校园图片继续从 `public` URL 使用并由组件提供显式尺寸，不需要构建期探测；未来只有上游发布安全版本并通过同一构建/浏览器/审计门禁后才允许恢复解析。
- 验证：干净 `npm ci`、`npm ls` 去重、ICNS/JXL/HEIF 快速拒绝、主站生产依赖 0、基础设施 28/28、生产构建、TypeScript 与浏览器 58/58 通过。本切片未部署，Linux PR CI 与 staging 镜像仍待执行。

## M7 已验证切片：auth-api 跨实例持久限流

- 新增 `0016_shared_rate_limits.sql` 和原子消费函数。凭据、密码重置、邮箱验证、头像上传、社区发布/互动/举报、教师评价写入/审核及管理员 MFA 改为 PostgreSQL 共享令牌桶；生产入口显式注入持久 store，普通高吞吐读写保留有界内存防突发。
- 桶主键只包含操作作用域和 32 字节 HMAC 摘要，不保存原始 IP、账号、用户或会话标识。消费在行锁内完成补充与扣减，拒绝也刷新最后访问时间；数据库无裁决时抛出异常并由 API 返回 503，不允许静默回退单机。每日清理只删除超过 24 小时未访问、早已完全补满的桶。
- 嵌入式 PostgreSQL 验证两个限流实例并发 20 次争抢容量 5 时恰好放行 5 次，新建实例仍继承耗尽状态，不同账号/IP 摘要和操作作用域互不影响，数据库故障与原始键失败关闭。验证：整仓 Node 111/111、auth-api 72/72、小影 33/33、基础设施 28/28、生产构建、TypeScript、CSS 审计、全仓 ESLint、主站生产依赖审计和 `git diff --check` 通过。本切片未部署；真实 PostgreSQL 17 并发与 runtime 权限仍是 staging 门禁。

## M7 已验证切片：小影公共入口持久限流

- 小影邀请码与图书馆公共配对提交不再使用进程内 Map；现有 SQLite 新增 `public_rate_limits`，每次消费在 `BEGIN IMMEDIATE` 写事务内完成，达到上限的请求也刷新最后访问时间。窗口与小影数据卷共同持久化，进程重启不会恢复攻击者配额。
- 网络标识在写库前使用小影主密钥做 HMAC-SHA256，数据库只保存 64 位摘要；邀请码和配对作用域独立。代理链从最靠近服务端的转发地址取值，避免攻击者通过伪造最左侧 `X-Forwarded-For` 绕过同一 IP 配额。超过 24 小时未访问且早已过窗的记录在受控事务内清理。
- 验证：同一标识达到上限、不同标识/作用域隔离、关闭并重开 SQLite 后继续拒绝、过窗恢复、库内无原始 IP 均通过；小影 34/34、基础设施 28/28、相关 ESLint 和 `git diff --check` 通过。本切片未部署；多个真实容器争用同一卷及 Caddy 转发链仍是 staging 门禁。

## M7 已验证切片：TraceInt 授权全链路 HTTPS

- 2026-08-09 对官方地址做无凭据探测，`https://wechat.v2.traceint.com/index.php/urlNew/auth.html` 与 HTTPS GraphQL 均原生返回 200；对应 HTTP 授权地址仍直接返回 200 而不强制升级，因此不能依赖跳转补救明文首跳。
- 内置授权端点改为 HTTPS，站长版本化协议配置只接受 HTTPS。授权客户端在每个手工重定向请求发出前同时校验协议和官方域名；上游若返回 HTTP 跳转，会在携带已收集 Cookie 发出下一跳前失败关闭。
- 验证：默认首跳 HTTPS、配置拒绝 HTTP、明文重定向只触发首个安全请求且不发送后续 Cookie；小影 35/35、基础设施 28/28、相关 ESLint 和 `git diff --check` 通过。SEC-004 已关闭；仍需单账号 staging 真实授权验证上游当前响应契约，本切片未部署。

## M9 已实现待环境验证切片：PostgreSQL 隔离恢复演练

- 新增 `ops/postgres/restore-drill.sh`：默认选择最新本地 custom-format 备份，先校验独立 SHA-256 与 30 小时新鲜度，再以固定前缀、UTC 时间和 PID 创建唯一 `template0` 临时数据库，使用 `pg_restore --exit-on-error` 完整恢复。
- 恢复后检查至少 16 个迁移记录、公共 schema 无未验证约束，并确认账号、会话、个人课表、社区、教师和共享限流关键表存在；输出不含凭据的行数摘要。退出陷阱只删除生成的临时库，恢复超过默认 900 秒 RTO 也判失败。新增每周日 systemd service/timer，参数只提供无真实值示例。
- 基础设施契约 28/28 和 `git diff --check` 通过；当前 Windows 开发机没有 `sh`、Docker、PostgreSQL 或 WSL，因此脚本尚未真实执行，不能把源码契约标记为恢复成功。SEC-009 仍因首次真实演练和异地备份凭据保持部分修复，本切片未部署。

## M8/M9 已实现待 CI 验证切片：原生 PostgreSQL 17 门禁

- GitHub quality workflow 新增独立 `postgres-17-integration` 作业，启动官方 `postgres:17-alpine` 健康服务、安装 PostgreSQL 客户端和锁定依赖，从空库运行仓库迁移器两次；第二次仍须通过 checksum 与幂等检查。
- 原生测试分别用 owner、auth runtime 和 importer 建立真实连接：要求服务端版本至少 17、迁移恰为 16 个；20 个 runtime 并发争抢容量 5 的共享桶只能放行 5 个，关闭连接池后仍拒绝同一摘要，不同摘要/作用域独立。runtime 必须不能直读限流表或 UPDATE 管理审计，importer 必须不能读取账号、会话、审核决定或执行共享限流函数。
- 原生作业现在还会安装 auth-api 锁定依赖并启动真实 HTTP server：两个随机普通账号和一个管理员候选通过 Argon2id 注册并取得数据库会话；普通用户访问管理员会话接口得到 403；账号 A 发布主题、账号 B 的跨账号改写不生效、B 回复后 A 收到并读取消通知。管理员候选通过真实 bootstrap 事务提升并撤销旧会话，重新登录后以真实 TOTP 创建绑定基础会话的短期提升 Cookie，再读取 B 对主题的不可变举报证据、入案、隐藏内容并核对全局 append-only 审计。测试使用真实 `createAuthStore`、运行角色连接、持久限流和 community/admin store，不注入内存假 store。
- 同一真实 HTTP 场景新增个人云同步与教师评价：账号 A 首次写入 revision 1，同 mutation 原样重试命中持久幂等记录，新 mutation 携带旧 revision 得到 409；账号 B 的快照仍为 revision 0。测试以 owner 仅插入一个最小公开教师事实，后续评价全部走 runtime API；A 创建评价后 B 的私有资源为 null，两个 `expectedVersion=1` 并发更新恰好一个 200、一个 409，公开列表仍只有一条活动评价。
- 一次性凭据也进入真实数据库并发门禁：同一邮箱验证令牌同时确认两次得到一个 200、一个 400，随后会话读取到已验证状态；管理员 bootstrap 后建立两个新基础会话，同一 TOTP 并发提升只允许一个会话成功，同一恢复码在同一会话并发消费也只有一个成功。当前只声明测试已编码，本机 SKIP 和未运行 CI 不作为原生并发通过证据。
- 原生角色测试从少量拒绝查询扩展为显式 ACL 矩阵：runtime 只能按产品需要读教师事实并写用户评价，不能写教师来源身份或直读导入/候选/决定，社区编辑历史、治理动作和全局管理员审计只能追加，主题/评论不能硬删除；审核白名单函数可执行，但底层提权检查和 importer 回滚函数不可直接调用。importer 只能维护导入批次、逐行证据、教师事实和私有候选，只读公开评价，不能读账号/会话、写公开评价/候选决定/管理员审计或调用审核/限流函数，只保留专用未批准候选回滚函数；角色属性锁定为 `NOINHERIT`、非 superuser/createdb/createrole/bypassrls。
- 社区证据与注销不变量已加入同一原生场景：报告创建后，使用数据库 owner 直接改 `evidence_body` 仍必须被不可变触发器以异常拒绝；治理隐藏并写审计后，内容作者从真实注销 API 删除账号，随后原生查询必须确认用户行归零、举报正文/作者标签快照未变、`admin.community.hide` 审计仍存在。该测试已编码但尚未在真实 PG17 作业运行。
- 原生 PG17 作业新增完整恢复演练步骤：固定安装 PostgreSQL 17 客户端，现场生成无 owner/ACL 的 custom-format 备份和 basename SHA-256；`restore-drill.sh` 新增显式 `native` 模式以复用同一校验/恢复/清理逻辑，生产默认 `docker` 模式不变。CI 以 120 秒 RTO 恢复到唯一临时库，核对迁移、约束、关键表和行数摘要，脚本成功后再查询 `pg_database` 确认无演练库残留。该作业尚未推送运行，SEC-009 仍不能关闭，异地副本也仍受凭据阻塞。
- 本地 auth-api 72/72、基础设施契约 29/29、集成测试加载 2 项明确 SKIP、相关 ESLint 和 `git diff --check` 通过。当前 Windows 没有 PostgreSQL，且分支尚未推送，不能宣称 GitHub PostgreSQL 作业或该真实 HTTP 流程已经成功；首次全绿后才可更新 SEC-002/003/006/011/013 的原生证据，本切片未部署。

## M9 已实现待 CI 验证切片：生产 Linux 镜像门禁

- quality workflow 新增独立 `linux-production-images` 作业，在 Ubuntu runner 分别从根目录、`services/auth-api` 和 `xiaoying-executor` 的真实生产 Dockerfile 构建主站、账号 API 与小影镜像，不复用宿主机 `node_modules`，也不以普通 Node 构建替代容器构建。
- 三个 Dockerfile 保留生产默认的 DaoCloud Node 22 Alpine 基础镜像，同时通过统一 `NODE_IMAGE` 构建参数允许 CI 显式使用官方 `node:22-alpine`，避免镜像源网络差异改变生产默认值。门禁逐一检查最终镜像运行用户必须为 `node`；auth-api 镜像实际加载 musl 架构下的 Argon2id、Sharp 与 PostgreSQL 驱动，小影加载运行依赖，主站核对失败关闭的 `image-size@2.0.3-dufesh.0` 依赖树并启动真实生产网关接受 HTTP 请求。
- 本地基础设施契约 30/30、相关 ESLint 和 `git diff --check` 通过。当前 Windows 没有 Docker，三个镜像尚未在本机或 GitHub runner 构建，不能把已编码的作业记为 Linux/musl 验证通过；首次 CI 全绿后才能勾选部署检查表的镜像构建与原生依赖项目，本切片未部署。

## 下一步

1. 首次运行现有 Linux 镜像、PostgreSQL 17 与隔离恢复 CI 门禁；随后补治理事务故障回滚竞态、教师同名隔离、导入/审核并发和 migrator/backup 权限边界，再完成两个普通用户与一个管理员的浏览器 E2E。
2. 生产邮件发送适配器和校园账号验证仍待凭据/上游能力；保持失败关闭，不阻塞 M2 开发。
3. M6 本地切片已完成；具备 staging 条件后实测 LCP、INP、CLS，并以真实登录态复核长会话、服务端列表游标和校园照片解码。
4. M3 本地实现已完成；在原生 PostgreSQL 17 验证 importer ACL、公开读取、评价提交/编辑/删除、账号注销撤下、审核/回滚并发锁和完整回滚。未审核历史正文仍不得公开。
