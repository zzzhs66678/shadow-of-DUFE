# 东财之影正式发布进度

最后更新：2026-08-03

## 当前状态

- 分支：`codex/release-upgrade`
- 当前里程碑：M0 收尾，下一步 M1 账号数据隔离与真实认证。
- 最近提交：`bdfc6d3 feat: establish the V18 campus visual baseline`
- 生产状态：本轮代码未部署；正式站仍运行既有版本。
- 用户未纳入版本控制的 `Website_Information_Form.doc` 与 `待上传资料/` 保持原样。

## 状态定义

- **已验证**：功能已实现，并有自动化或明确人工验收证据。
- **已实现待凭据**：代码与本地/测试验证完成，只差生产密钥或第三方资质。
- **已实现但测试失败**：代码存在，但当前不能作为可交付能力。
- **未实现**：尚未开始或仅完成设计。
- **外部阻塞**：只能由生产凭据、验证码、实名认证、付费资源或不可逆操作解除。

## 里程碑总览

| 里程碑 | 状态 | 已有证据 | 下一步 |
|---|---|---|---|
| M0 审计与计划 | 进行中 | 仓库、页面、技术栈、部署、工作簿、多角色审计和基线测试完成 | 提交五份执行文档 |
| M1 账号/同步/管理 | 未实现 | 既有匿名会话、OAuth mock、个人同步底座与 10 项 API 测试可复用 | 先修跨账号本地数据污染 |
| M2 资料检索 | 未实现 | 已定位现有错误跳入选课流和 457 条资料清单 | 建独立资料 API/路由 |
| M3 教师/导入 | 未实现 | 已审计用户工作簿结构与风险 | 设计迁移和预检脚本 |
| M4 社区/通知/审核 | 未实现 | 无现有服务端模型 | 在认证、RBAC、teacher_id 后实施 |
| M5 设计/文案 | 部分已验证 | V18 基线提交；首页克制、照片在“我的” | 清理 CSS 叠层和交互残留 |
| M6 移动/无障碍/性能 | 部分已验证 | 35 个页面×宽度组合无页面级横向溢出 | 补触控、焦点、axe、数据拆分 |
| M7 安全 | 进行中 | 初始威胁模型与问题清单完成 | 随各里程碑修复并复测 |
| M8 全量测试 | 未实现 | 现有回归基线可用，但主要 UI 测试仍为源码断言 | 建浏览器 E2E 与迁移测试 |
| M9 部署准备 | 未实现 | 既有 Docker/Caddy/原子发布流程可复用 | staging 前完成全部门禁 |
| M10 PR/交付 | 未实现 | 独立功能分支已建立 | 全部里程碑后推送与建 PR |

## M0 基线证据

### 已验证

| 检查 | 结果 |
|---|---|
| ESLint：`app services tests scripts xiaoying-executor/bin xiaoying-executor/src` | 通过 |
| 课程/体验/基础设施/个人同步回归 | 31/31 通过 |
| auth-api 测试 | 10/10 通过 |
| 小影测试 | 26/26 通过 |
| 生产构建与 SSR | 构建成功，2/2 SSR 回归通过 |
| 主站生产依赖审计 | 0 个已知漏洞 |
| 页面响应式检查 | 320/360/375/390/414/768/1280px × 5 个主视图，共 35 组无页面级横向溢出 |
| Git 工作区 | 已识别并保护用户未跟踪文件；开发分支独立于 `main` |

### 尚待重试

- auth-api 与小影的独立 `npm audit` 在本机两次遇到 npm registry TLS 连接中断；这是未验证，不记为通过。CI 与后续本地复测都必须重跑。

## M0 审计结论

### 发布阻断

1. 退出账号后本地 `saved` 未按账号隔离；同浏览器 B 账号可能把 A 的本地数据合并上传。
2. 现有账号服务只有 disabled/mock OAuth，没有普通用户可用的真实注册登录。
3. 没有服务端 RBAC、管理员 MFA、管理 API 和管理员审计。
4. 资料清单没有进入真实搜索路径，点击课程会进入教学班选择。
5. 不存在 `teacher_id`、社区、通知、举报和审核数据模型。
6. TraceInt 的授权码与 Cookie 交换当前使用明文 HTTP，需要确认上游能力并消除或隔离链路风险。
7. 关键 UI 测试以源码正则为主，不能证明键盘、焦点、移动交互和权限真实可用。

### 前端与体验风险

- `DufeHubV2.tsx` 超过 5,000 行，页面、数据、弹层与路由耦合。
- 三层 CSS 含多个历史覆盖段；当前约 248KB 客户端 CSS，需在 M5 收敛。
- 首页启动即读取约 3.07MB 课程 JSON 与约 259KB 资料清单，需在 M6 拆成轻量入口数据。
- 搜索文案承诺方向键和 Enter，但现有实现只有 Escape。
- 多个弹层缺完整焦点锁定；隐藏抽屉仍可能留在可访问树。
- 部分手机触控目标只有 34–40px；今日主 CTA 在 390×844 首屏以下。

### 教师工作簿审计

- 源文件：用户提供的 `教师评价（学长学姐）.xlsx`，未复制进仓库。
- 1 个工作表，38 位教师，2 个学院，135 条非空历史评论；5 位教师没有评论。
- 未发现手机号、身份证号、邮箱、URL 或明显社交账号格式。
- 发现至少一条直接辱骂，以及若干无法核验的个人化或考核结论；原始内容不得直接上线。
- 导入时必须增加来源、批次、内容摘要、脱敏/审核状态，并统一标记为“历史整理内容”。

## 下一步

1. 提交 M0 文档与 `MASTER_RELEASE_BRIEF.md`。
2. 为匿名和账号数据建立独立存储命名空间，加入 A→退出→B 防串号测试。
3. 修复本地 auth API 不可用时“我的”页永久显示加载中的问题。
4. 设计并提交身份/RBAC 增量迁移，再实现真实凭据认证。
