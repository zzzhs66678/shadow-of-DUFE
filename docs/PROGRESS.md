# 东财之影正式发布进度

最后更新：2026-08-09

## 当前状态

- 分支：`codex/release-upgrade`
- 当前里程碑：M5 设计系统与文案；正在人工收敛自动审计无法判断的公共结构和交互原语，M4 的 PostgreSQL 17 staging 验证仍待可用环境。
- 已完成提交：`bdfc6d3` 视觉基线、`91a296e` 发布计划、`c16fe40` 账号本地数据隔离、`9d108c4` 凭据认证、`b6514b4` 账号资料管理、`1d46813` 安全头像处理、`9da33ac` 管理员安全底座、`5315960` 管理员 RBAC/API、`d7fc830` 管理员值守台、`ef8675e` 邮箱验证、`7f815f0` 资料检索 API、`3bffd6e` 独立资料档案、`4337290` 社区数据底座、`324c59c` 社区公开读取 API、`452efd0` 社区主题/评论写入、`7e8994e` 社区互动与举报、`89abc3a` 社区通知闭环、`437bc36` 管理员社区治理。
- 生产状态：本轮代码未部署；正式站仍运行既有版本。
- 用户未纳入版本控制的 `Website_Information_Form.doc` 与 `待上传资料/` 保持原样。

## 状态定义

- **已验证**：功能已实现，并有自动化或明确人工验收证据。
- **已实现待凭据**：代码与本地/测试验证完成，只差生产密钥或第三方资质。
- **已实现但测试失败**：代码存在，但当前不能作为可交付能力。
- **未实现**：尚未开始或仅完成设计。
- **外部阻塞**：只能由生产凭据、验证码、实名认证、付费资源或不可逆操作解除。

## 里程碑总览

| 里程碑 | 状态 | 已有证据 | 下一步 |
|---|---|---|---|
| M0 审计与计划 | 已验证 | 仓库、页面、技术栈、部署、工作簿、多角色审计、基线测试与五份执行文档已提交 | 已完成 |
| M1 账号/同步/管理 | 进行中 | 账号本地数据分区；凭据注册登录；Argon2id；失败锁定；一次性重置；资料白名单更新；安全头像；邮箱验证令牌闭环；管理员 TOTP/恢复码/独立提升 Cookie/管理 API/值守台/审计；运行角色配置；auth-api CI | 在 PostgreSQL 17 复核迁移/权限/并发，接生产邮件与校园账号验证 |
| M2 资料检索 | 已实现并本地验证 | 457 条资料实体、独立 API/页面/详情、URL 搜索状态、直接预览下载、异常状态、键盘与 390px 手机路径 | staging 挂载真实资料卷后抽样预览/下载与多宽度 E2E |
| M3 教师/导入 | 待新版资料 | 用户确认现有教师与课程教材工作簿仍在修改，本轮未导入、未提交、未固化字段映射 | 收到完整版后从 dry-run 重新开始 |
| M4 社区/通知/审核 | 已实现并本地验证 | 普通用户读写、互动、屏蔽、举报、通知、独立社区页面，以及管理员入案、不可变证据、内容/用户治理、作者通知、双审计与工作台已实现；148/148 本地测试通过 | 在真实 PG17 验证迁移/事务/权限，并用两个用户和一个管理员完成 staging E2E |
| M5 设计/文案 | 进行中 | 统一 `--dufe-*` 色彩事实；资料/社区/后台接入同一纸墨朱金松针；首页移除致敬区；账号、资料、社区文案收紧；70 条等价规则和 1,262 条被后层必然覆盖的普通声明已安全裁剪并加入自动审计；资料、详情与社区已共用公共 Masthead | 继续人工合并剩余差异化历史覆盖，统一按钮、表单和弹层原语 |
| M6 移动/无障碍/性能 | 部分已验证 | 35 个页面×宽度组合无页面级横向溢出；资料页不再移动端抢焦点；社区/后台弹层适配动态视口与安全区；关键小目标补至 44px | 补 320—768px、横屏、axe、数据拆分和性能预算 |
| M7 安全 | 进行中 | 初始威胁模型与问题清单完成 | 随各里程碑修复并复测 |
| M8 全量测试 | 未实现 | 现有回归基线可用，但主要 UI 测试仍为源码断言 | 建浏览器 E2E 与迁移测试 |
| M9 部署准备 | 未实现 | 既有 Docker/Caddy/原子发布流程可复用 | staging 前完成全部门禁 |
| M10 PR/交付 | 未实现 | 独立功能分支已建立 | 全部里程碑后推送与建 PR |

## M0 基线证据

### 已验证

| 检查 | 结果 |
|---|---|
| ESLint：`app services tests scripts xiaoying-executor/bin xiaoying-executor/src` | 通过 |
| 课程/体验/基础设施/个人同步回归 | 31/31 通过 |
| auth-api 测试 | 10/10 通过 |
| 小影测试 | 26/26 通过 |
| 生产构建与 SSR | 构建成功，2/2 SSR 回归通过 |
| 主站、auth-api、小影生产依赖审计 | 均为 0 个已知漏洞 |
| 页面响应式检查 | 320/360/375/390/414/768/1280px × 5 个主视图，共 35 组无页面级横向溢出 |
| Git 工作区 | 已识别并保护用户未跟踪文件；开发分支独立于 `main` |

## M1 已验证切片：账号本地数据隔离

- 旧版 `dufesh:student-profile:v2` 只迁移到匿名作用域，不会自动进入任一登录账号。
- 匿名数据和每个 `user_id` 使用不同本地键；同步基线元数据也按用户分区。
- 退出或注销后清除当前账号的本地缓存与同步元数据，再恢复匿名作用域；不会显示或上传前一账号的数据。
- 登录后检测到匿名数据时只显示显式选择，“导入当前账号”前不会合并；“暂不导入”保留匿名数据。
- 本地 auth API 缺失或返回非成功状态时，账户中心进入匿名/离线状态，不再永久停在加载中。
- CI 已加入 auth-api 安装、生产依赖审计、10 项服务测试，以及个人存储/同步回归。
- 验证：账号存储/同步 9/9、体验与基础设施合计 22/22、auth-api 10/10、构建与 SSR 2/2；390px 浏览器页面无横向溢出。

## M1 已验证切片：用户名/邮箱/密码认证

- 新增增量迁移 `0006_credential_auth.sql`：用户唯一规范化用户名/邮箱、可选校园账号与验证时间、注册来源、最后登录时间、密码凭据和密码重置令牌。
- 密码使用真实 Argon2id 哈希；注册做 Unicode NFKC、字段白名单、长度、常见口令和身份派生口令检查，不保存明文。
- 登录对未知账号和错误密码返回同一结果；按 IP+规范化账号限流，服务端记录失败次数并在第 5、6、7 次分别进入 30 秒、2 分钟、15 分钟锁定。
- 注册和登录每次签发新会话；一次性重置令牌只存 HMAC 摘要、30 分钟过期、原子消费，重置成功撤销全部旧会话。开发模式可以把一次性码返回给本地测试；生产配置禁止该模式。
- “我的”页已有登录、创建账号、密码强度说明和重置状态；微信入口降为审核状态，不冒充可用。390×844 与 1440×900 浏览器检查均无页面级横向溢出，桌面首轮发现并修复了全站 footer 样式污染认证面板的问题。
- 验证：auth-api 16/16，包含真实 Argon2id、注册/冲突、统一失败、锁定、登录、单次重置与旧会话撤销；auth-api 生产依赖 high/critical 0；相关源码 ESLint 通过；主站构建与 SSR 2/2 通过。
- 未验证：当前机器无 Docker/PostgreSQL，`0006` 尚未在 PostgreSQL 17 空库、升级库和重复执行中运行；生产重置邮件缺发送服务凭据。本切片未部署。

## M1 已验证切片：账号公开资料底座

- `GET/PUT /api/auth/profile` 只允许当前有效会话读取和修改自己的资料；写入额外要求可信 Origin。
- 可修改字段严格限定为用户名、显示名和校园账号；`status`、角色、邮箱验证等字段无法通过请求体越权写入。
- 用户名做 NFKC 规范化和数据库唯一约束；校园账号更改后服务端清除原验证时间，界面明确显示其验证状态。
- 邮箱、注册时间、最后登录时间、账号状态、可选校园账号及验证状态已进入当前账号的会话/资料模型；“我的”页可编辑资料，不把未验证校园账号包装成真实身份。
- 验证：auth-api 18/18、相关源码 ESLint、主站生产构建通过；尚未实现邮箱/校园账号验证投递。

## M1 已验证切片：安全头像处理

- 新增增量迁移 `0007_user_avatars.sql`，头像与用户一对一并随账号级联删除；数据库只接受 512×512、512KB 以内的 WebP 及其 SHA-256 摘要。
- 上传只接受 JPEG、PNG、WebP，原图上限 5MB、最大边长 4096、最大 2000 万像素；服务端复核文件魔数和声明 MIME，拒绝动图/多页文件，纠正方向后裁切并重新编码为 WebP。
- 原始文件、EXIF 和定位信息不落库；头像写入和删除要求有效会话、可信 Origin，并按账号与 IP 限流。公开读取只返回活跃账号的头像，使用版本号、ETag、最多 5 分钟缓存与 `nosniff`，避免删除后长期保留旧副本。
- “我的 → 账号资料”已接入头像更换与删除，客户端提前提示格式和大小，但安全边界仍由服务端执行；隐私政策同步说明处理方式及公开展示属性。
- 验证：auth-api 21/21（含真实 Sharp 解码/重编码、伪造 MIME、随机无效内容、体积上限、读取缓存与删除闭环）；本机 `npm ci --ignore-scripts` 后原生模块仍可加载。真实 PostgreSQL 17 迁移和 Linux musl 镜像尚待 staging/CI 验证，本切片未部署。

## M1 已验证切片：邮箱验证令牌闭环

- 新增 `0009_email_verification.sql`：只保存一次性令牌的 HMAC 摘要、申请时的规范化邮箱、过期和消费时间；账号删除时级联清理，过期/已消费记录 30 天后由维护任务删除。
- 请求验证要求有效登录、可信 Origin、未验证邮箱及 IP+账号限流；新请求使该账号旧令牌失效。确认要求同一登录账号，令牌只能在账号仍活跃、邮箱未变化、未过期且未消费时原子成功一次。
- “我的 → 账号资料”已接入请求、一次性令牌确认、错误说明和账号状态刷新；生产不开放调试令牌回传，投递通道未配置时只显示“待开通”，不伪造邮件发送。
- 本地 `response` 模式覆盖了不可信 Origin、跨账号拒绝、旧令牌失效、成功确认、重复确认拒绝和会话状态更新；生产环境配置为 `response` 时启动失败。
- 验证：auth-api 32/32、相关 ESLint、基础设施/体验测试与生产构建通过。真实 PostgreSQL 17 迁移和生产邮件适配器仍未验证；校园账号验证未实现，本切片未部署。

## M1 已验证切片：管理员加密与 MFA 数据底座

- 新增增量迁移 `0008_admin_security.sql`：服务端角色、TOTP 因子、单次恢复码、绑定基础会话的短期提升会话和 append-only 管理审计事件。
- TOTP 按 RFC 6238 的 SHA-1、6 位、30 秒与 ±1 时间步验证；匹配后的时间步将由数据库条件更新原子消费，阻断并发重放。密钥使用 AES-256-GCM，加密 AAD 绑定因子 ID、用户 ID 与用途；key ID/keyring 支持逐步轮换。
- 恢复码由 128 位随机数生成，明文只在初始化时显示一次，数据库只存独立 `AUTH_ADMIN_RECOVERY_PEPPER` 的域分离 HMAC 摘要，不复用普通会话 pepper。
- 管理员默认关闭，只能用 `npm run admin:bootstrap -- <username-or-email>` 将明确的已有活跃账号初始化；不会自动提升首个注册者。重复初始化必须显式 `--rotate`，且会撤销该账号全部基础/提升会话并写系统审计。
- 验证：auth-api 26/26，包含 RFC 向量、密文不含明文、AAD 绑定、TOTP 时间窗/重放、恢复码规范化与 pepper 隔离、配置失败关闭；真实 PostgreSQL 迁移/原子并发、运行期数据库角色拆分、管理 API 与界面尚未完成，本切片未部署。

## M1 已验证切片：管理员 RBAC 与提升会话 API

- `/api/admin/session`、`/elevation`、`/overview`、`/users`、`/audit` 和用户状态写入均由服务端实时角色授权；匿名返回 401，普通用户在任何管理查询前返回统一 403，不根据对象是否存在产生差异。
- TOTP 或恢复码成功后签发独立 `__Host-`、HttpOnly、Secure、SameSite=Strict 提升 Cookie，固定最长 10 分钟且绑定当前基础会话；另一会话携带同一 Cookie 仍被拒绝。退出、密码重置、当前设备撤销、账号禁用与删除均清除或使提升失效。
- TOTP 时间步和恢复码在创建提升会话的同一事务中条件消费；相同 TOTP 时间步或恢复码只能成功一次。MFA 按 IP+用户+基础会话独立限流，Origin 缺失或不可信时不读取 MFA 数据。
- 用户列表只返回掩码邮箱和必要状态；账号禁用、全部会话撤销、提升撤销与包含脱敏前后状态/原因的审计事件同事务提交。写操作进入事务后再次验证管理员、基础会话与提升会话，避免授权检查后的竞态。
- 迁移器已支持创建独立 `AUTH_DB_USER` 非 owner 运行角色；运行角色可读写业务表，但不能更新/删除/截断 `admin_audit_events`，也不能访问 `schema_migrations`。Caddy 为 `/api/admin/*` 增加 no-store、noindex 同源代理。

## M1 已验证切片：管理员值守台

- 新增受保护的 `/admin`：未登录、普通用户、未配置 MFA、提升过期和连接失败均有独立状态，管理数据只在服务端确认管理员角色和有效短时提升后请求。
- TOTP 与恢复码使用同一明确输入，不在界面、日志或本地存储保留；值守台显示短时权限截止时间并支持主动结束提升会话。
- 概览、掩码账号列表、用户名/邮箱搜索和 append-only 审计时间线已接真实 `/api/admin/*`；停用/恢复必须填写至少 8 个字原因，并发送当前状态作为乐观锁，冲突后重新拉取。
- 停用确认明确说明全部既有会话会撤销；恢复确认明确说明旧会话不会复活。操作入口仅对会话角色为 admin 的用户显示，但直接访问仍由服务端 401/403/MFA 三层检查。
- 页面 metadata 与 robots 均禁止抓取；本地开发新增同源 auth/admin API 代理，可用 `AUTH_API_DEV_TARGET` 指向独立认证服务。源码 ESLint、基础设施/体验测试和生产构建通过。
- 浏览器视觉复核：桌面完整值守簿、390px 手机值守簿、390px MFA 入口及停用确认层均已检查；复核使用本机临时只读模拟响应，只证明渲染和交互状态，不替代真实管理员、Cookie 与 PostgreSQL E2E。
- 验证：auth-api 31/31，包含普通用户 403 且敏感 store 0 调用、严格 Cookie、跨会话拒绝、TOTP 重放、恢复码单次使用、恶意 Origin、独立限流、脱敏列表、禁用/会话撤销/审计闭环；全部 58 项仓库回归、ESLint、主站构建与 auth-api 生产依赖审计通过。真实 PostgreSQL 事务并发、角色权限与 shell 脚本因本机缺少环境尚未验证，真实账号浏览器 E2E 尚未完成，本切片未部署。

## M2 已验证切片：资料检索数据契约与只读 API

- 457 份已发布文件全部升级为带稳定 `material_id` 的资料实体；保留原预览/下载 URL，并从课程事实补齐课程 ID、教师、学院、学期、年级、类型和标签索引。当前 29 个资料目录均能匹配目录课程，没有按同名课程合并教学班。
- 新增公开只读 `GET /api/materials` 和 `GET /api/materials/:material_id`；列表支持资料标题、课程/课程号、教师、标签、类型、学期、年级及有界分页，响应只包含资料实体；未知或撤下 ID 明确返回 404，不回退到课程/选课流程。
- 检索查询做 NFKC 规范化、长度限制，分页每次最多 60 条；服务端搜索字段不会泄露进公开响应。静态元数据使用短期公开缓存，文件继续由既有 `/resources/files/*` 只读路径提供，旧 URL 不变。
- 独立 `/materials` 资料档案和 `/materials/:material_id` 详情已接入真实 API；课程库“搜索资料”与全站资料结果直接进入资料路由，不再打开教学班抽屉。结果行直接提供预览/下载，详情通过同源 HEAD 区分可用、404/410、403 与网络无法确认。
- 首次、加载、无结果、网络失败、资料不存在和文件不可用状态均有明确下一步；搜索词和课程、教师、类型、标签、学期、年级写入 URL。方向键可连续浏览结果，Enter 打开，Escape 清空并回到搜索框；reduced-motion 下取消位移与加载动画。
- 浏览器在 390×844 实际验证无横向溢出；“高数”得到 1 份资料，URL 同步，方向键聚焦并由 Enter 进入 `高数下.pdf` 详情；多结果连续方向键和 Escape、无结果状态均通过。桌面页视觉复核通过。
- 验证：资料领域 4/4、构建后 API/详情集成 1/1、全仓 107/107、auth-api 32/32、ESLint 和 vinext 生产构建全部通过。生产构建可识别资料列表 API、详情 API、资料页和动态详情页。本机未挂载正式 `/srv/resources`，真实文件抽样仍待 staging；本切片未部署。

## M0 审计结论

### 发布阻断

1. 退出账号后本地 `saved` 未按账号隔离；同浏览器 B 账号可能把 A 的本地数据合并上传。
2. 现有账号服务只有 disabled/mock OAuth，没有普通用户可用的真实注册登录。
3. 没有服务端 RBAC、管理员 MFA、管理 API 和管理员审计。
4. 资料清单没有进入真实搜索路径，点击课程会进入教学班选择。
5. 不存在 `teacher_id`、社区、通知、举报和审核数据模型。
6. TraceInt 的授权码与 Cookie 交换当前使用明文 HTTP，需要确认上游能力并消除或隔离链路风险。
7. 关键 UI 测试以源码正则为主，不能证明键盘、焦点、移动交互和权限真实可用。

### 前端与体验风险

- `DufeHubV2.tsx` 超过 5,000 行，页面、数据、弹层与路由耦合。
- 三层 CSS 含多个历史覆盖段；当前约 248KB 客户端 CSS，需在 M5 收敛。
- 首页启动即读取约 3.07MB 课程 JSON 与约 259KB 资料清单，需在 M6 拆成轻量入口数据。
- 搜索文案承诺方向键和 Enter，但现有实现只有 Escape。
- 多个弹层缺完整焦点锁定；隐藏抽屉仍可能留在可访问树。
- 部分手机触控目标只有 34–40px；今日主 CTA 在 390×844 首屏以下。

### 教师工作簿审计

- 用户已确认当前教师评价和课程教材工作簿仍在修改；该文件仅视为未完成草稿。
- 草稿未复制进仓库、未导入数据库，先前局部工作表统计不得作为正式验收或导入基线。
- 收到完整版后重新执行结构、重名、课程号、教材版本、隐私与内容风险 dry-run；结果经确认前不创建教师身份、不覆盖教材事实、不公开历史评论。

## M4 已验证切片：社区、通知与治理数据库底座

- 新增增量迁移 `0010_community_foundation.sql`：主题、评论与一层回复、编辑历史、主题/评论点赞、收藏、用户屏蔽、提及和站内通知。
- 评论层级由数据库触发器限制为“根评论 + 一层回复”；主题和评论只允许软删除，硬删除由触发器与数据库权限双重拒绝，外键使用 `RESTRICT`/`SET NULL` 保留引用，通知额外保存 `fallback_path`，目标删除后可进入降级页。
- 点赞、收藏、提及和开放举报均使用唯一键或部分唯一索引防重复；通知按收件人和幂等键去重，并为未读游标建立部分索引。
- 举报队列、审核案件、案件—举报关系、用户制裁和治理动作已建模；治理动作与内容编辑历史使用 append-only 触发器，并从 auth runtime 撤销更新、删除和截断权限。
- 不可变历史只保留去标识化用户 UUID/显示快照，不使用会在账号注销时回写的外键；举报、案件与制裁证据不会因用户注销被级联删除，也不会反向阻断账号注销。回复的主题/父级/根级插入后不可重挂，案件—举报和审核动作必须与同一目标一致，通知降级路径只接受站内绝对路径。
- 验证：基础设施契约 19/19、全仓 Node 测试 109/109、`git diff --check` 通过。当前机器没有 PostgreSQL/Docker，真实 PostgreSQL 17 空库、升级、重复迁移、触发器和权限矩阵仍待 staging；尚无社区 API 或用户界面，本切片未部署。

## M4 已验证切片：社区公开读取 API

- 新增 `GET /api/community/topics`、`GET /api/community/topics/:id` 与 `GET /api/community/topics/:id/comments`，由同域 Caddy 和本地 Vite 代理进入 auth-api；匿名用户可读取公开内容，登录用户响应额外包含自己的点赞与收藏状态。
- 主题和根评论使用有界 keyset 游标分页，每页最多 30 条；游标做长度、字符集和结构校验，当前只开放真实实现的 `latest` 排序，不伪造热门算法。
- 双向屏蔽关系从列表和详情隔离；已删除主题返回 410 和本站 `/community` 降级路径，已删除或屏蔽评论保留线程位置但不返回正文和作者。
- 新增增量迁移 `0011_community_read_paths.sql`，为包含软删除墓碑的根评论与回复读取建立索引，不修改已提交的 `0010`。
- 验证：auth-api 39/39、基础设施契约 20/20、全仓 Node 测试 117/117、相关 ESLint 通过。真实 PostgreSQL 17 查询计划、迁移和浏览器页面尚未验证；写入、通知与审核 API 尚未实现，本切片未部署。

## M4 已验证切片：主题与评论写入 API

- 登录用户可发布、编辑和软删除自己的主题、根评论与一层回复；全部写入要求可信 Origin、有效站内会话，并同时消耗用户和 IP 两个独立专项限流键。
- 主题和评论按纯文本契约做 NFKC 规范化、长度/控制字符限制与额外字段拒绝；编辑和删除必须提交当前版本，过期写入返回当前版本冲突，不执行覆盖。
- 写事务先检查账号状态和有效社区制裁；非作者修改统一返回不存在。主题或回复目标不可用、与作者存在双向屏蔽时拒绝新评论。
- 对二层回复再次回复时，服务端将新回复挂到原根评论，并保留实际 `reply_to_user_id`；不会尝试向数据库写入第三层。编辑和删除在同一事务先保存不可变旧版本，再更新正文或软删除墓碑；冲突事务不会留下历史记录。
- 验证：auth-api 47/47、全仓 Node 测试 125/125、基础设施契约 20/20、相关 ESLint 与 `git diff --check` 通过。真实 PostgreSQL 17 的事务、约束和并发仍待 staging；互动、举报、通知、管理 API 与社区页面尚未实现，本切片未部署。

## M4 已验证切片：互动、屏蔽与举报 API

- 主题/评论点赞、主题收藏和用户屏蔽使用明确的幂等 `PUT`/`DELETE`；唯一约束继续作为最终防刷边界。新增互动前复核账号封禁、目标状态和双向屏蔽，取消互动不会借响应泄露不可见内容的计数。
- 拉黑禁止指向自己；事务同时写入屏蔽关系、移除双方在彼此内容上的遗留点赞、移除拉黑者对对方主题的收藏，并将对方发给拉黑者的既有通知设为隐藏。屏蔽关系继续由公开列表、详情、回复和互动查询共同执行。
- 举报只接受固定目标类型、UUID、原因码和有界纯文本说明；举报自己被拒绝。“其他”原因必须填写至少 8 字说明，重复的开放/审核中举报返回原记录，不改写原始原因和证据。
- 发布、互动和举报分别使用独立 token bucket，并都同时按账号和 IP 消耗键；举报桶为 5 次突发、每 5 分钟恢复 1 次。
- 验证：auth-api 53/53、全仓 Node 测试 131/131、基础设施契约 20/20、相关 ESLint 与 `git diff --check` 通过。真实 PostgreSQL 17 的唯一索引冲突、事务回滚和并发仍待 staging；通知、管理员审核与页面尚未完成，本切片未部署。

## M4 已验证切片：通知与两账号回复闭环

- 新建根评论时在同一事务向主题作者写 `topic_reply`；回复评论时向实际被回复用户写 `comment_reply`。自己回复自己、账号已失活和任一方向存在屏蔽时不生成通知。
- 主题和评论正文识别规范化 `@用户名`，单条最多 10 个不同账号；提及关系与通知唯一写入。回复收件人同时被提及时只保留回复通知，不制造重复提醒。
- 登录用户拥有通知游标列表、未读计数、单条已读、全部已读和隐藏接口；所有 SQL 均按 `recipient_user_id` 约束，通知列表额外过滤双向屏蔽。发件账号注销后不向浏览器返回去标识化 UUID。
- 主题或评论软删除会在同一事务把关联通知改成删除说明、清空正文并指向 `/community`，不保留失效深链或已删除内容预览。
- 两账号 API 测试真实启动 HTTP 服务并跑通：B 登录回复 A 的主题，A 的未读数从 0 变 1、列表出现 `topic_reply`，A 单条已读后未读数归零。store 测试分别覆盖回复通知 SQL、提及规范化/去重、收件人作用域、屏蔽过滤和软删除降级。
- 验证：auth-api 58/58、全仓 Node 测试 136/136、基础设施契约 20/20、相关 ESLint 与 `git diff --check` 通过。真实 PostgreSQL 17 事务和浏览器界面仍待 staging；管理员内容处理通知、公告与社区页面尚未完成，本切片未部署。

## M4 已验证切片：管理员举报与治理闭环

- 短期 MFA 提升管理员可读取开放/审核中举报、把举报纳入案件，并执行内容隐藏、恢复、软删除，用户警告、限时停发、封禁、解封和驳回。原因做 NFKC、8—1000 字与控制字符检查，动作字段严格白名单；内容/用户动作不混用，状态冲突返回 409，管理员不能制裁自己。
- 打开案件和执行动作均在事务中重新校验管理员角色、基础会话与提升会话；提升被撤销时在读取举报或目标之前回滚。跨站请求、普通用户和额外字段均不能进入敏感 store。
- 案件动作同时写社区 append-only 治理记录和全局 append-only 管理员审计。内容状态、关联举报结案、用户制裁、旧通知脱敏与作者处理结果通知和两份审计同事务提交；内容只做软删除。
- 验证：auth-api 63/63、全仓 Node 测试 141/141、基础设施契约 20/20、相关 ESLint 与 `git diff --check` 通过。真实 PostgreSQL 17 的部分唯一索引、触发器、事务回滚、并发与 runtime 权限仍待 staging；社区页面和管理员举报工作台尚未接入，本切片未部署。

## M4 已验证切片：社区页面、通知中心与审核工作台

- 新增独立 `/community` 和 `/community/topics/:topic_id`：最新主题游标列表、发布、详情、根评论/一层回复、编辑冲突、软删除、点赞、收藏、屏蔽与撤销、举报、通知列表/未读/单条与全部已读/隐藏均直接调用真实 API。社区不进入五项主导航，只从“我的”进入。
- 前端只有已经实现的时间排序，不伪造热门榜或虚假计数；加载、空、网络失败、内容不可用和登录状态不确定均给出明确下一步。通知深链只接受本站路径，弹层/抽屉具备滚动锁、焦点陷阱、Escape 关闭和焦点恢复。
- `/admin` 在管理员完成短期 MFA 后加载举报队列；可查看举报时刻的不可变证据、入案并只执行服务端返回的允许动作。隐藏内容只能恢复或删除，生效制裁中的账号只能解封，且服务端事务再次校验同一矩阵；刷新失败不会把已成功的高权限写操作误报为失败。原因与期限在前后端共同校验，动作完成后刷新队列和 append-only 审计。
- 新增 `0012_community_report_evidence.sql`，举报事务保存不可变证据快照；旧举报回填后由触发器拒绝修改快照。审核队列同时返回目标当前状态、有效制裁与状态安全动作，避免界面提供必然 409 或无法恢复的操作。
- 浏览器在 1280 桌面和 390×844 手机验证；手机无横向溢出。后端断开时真实显示重试错误；临时内存响应只用于走通列表、点赞、详情、回复、通知、举报长度、滚动锁和焦点恢复，不作为数据库证据。
- 验证：auth-api 67/67、基础设施契约 21/21、全仓 Node 测试 148/148、构建后渲染 4/4、相关 ESLint 与 `git diff --check` 通过。真实 PostgreSQL 17、Linux 容器静态资源、真实 Cookie/管理员和并发 E2E 仍待 staging；本切片未部署。

## M5 已验证切片：统一色彩事实、内容归位与用户文案

- 建立全局 `--dufe-*` 语义色彩事实，`--journal-*`、`--ds-*`、资料、社区和后台局部变量改为兼容别名；修复“我的”社区入口引用社区模块私有变量而丢失朱线的问题。
- 新建个人日程默认朱红；旧 `blue` 数据键继续兼容，但显示为“炭墨”并实际渲染炭墨色。旧版今日和空教室兼容样式中的明确蓝色值也替换为当前纸墨朱色。
- Sci-Hub/Alexandra Elbakyan 致敬区从今日学习台移到“我的”校园影集之后；创作者“会一直有人管”“2024—∞”改为可核实的课余共创与“始于 2024”。
- 资料页标题改为具体搜索能力，进入手机页面不再自动聚焦并弹出键盘；账号不可用状态不再暴露“生产配置/本地开发/资质审核”等内部措辞；社区举报、屏蔽与编辑反馈改为用户能直接理解的结果。
- 社区/后台弹层增加 `100dvh`、安全区、内部滚动和 `overscroll-behavior`；社区手机操作与日程色板补齐 44px 触控目标，账号动画补全减少动态偏好。
- 验证：三角色只读审查完成；全仓 Node 150/150、构建后渲染 4/4、相关 ESLint 与 `git diff --check` 通过。浏览器验证桌面“我的”内容顺序和共享 Token、手机资料页不抢焦点且无横向溢出、日程默认朱红并将旧蓝键显示为炭墨。本切片未部署；历史三层全局 CSS 仍待分批裁剪。

## M5 已验证切片：全局 CSS 等价去重

- 新增 `scripts/audit-css-cascade.mjs` 与 `npm run audit:css`：解析三个全局样式表的媒体查询、支持查询、关键帧和嵌套祖先上下文，只把“同上下文、同选择器、同完整声明”的规则视为等价副本；修复模式只删除较早副本并保留最后生效位置。
- `globals.css`、`product-system.css`、`red-access-system.css` 共移除 70 条早期等价副本，减少 401 行、6,724 字节源文件；没有把声明不同的 V14—V18 覆盖混作重复项。新增 Node 回归阻止相同级联债务重新进入。
- 验证：CSS 审计通过；全仓 Node 151/151、构建后渲染 4/4、相关 ESLint 与 `git diff --check` 通过。浏览器在 1280px 桌面与 390×844 手机逐一打开今日、找课、课表、空教室和我的，全部无横向溢出；手机资料页仍不抢焦点，社区页沿用统一朱红 Token，两页均无横向溢出，控制台无页面错误。本切片未部署。

## M5 已验证切片：逐属性清除必然失效的历史覆盖

- CSS 审计扩展为按三个全局样式表的真实导入顺序检查：只有祖先媒体/支持/关键帧上下文完全相同、选择器文本完全相同、属性名完全相同，且后层声明按 `!important` 规则必然获胜时，才删除较早的普通声明。
- 自定义属性一律排除，保证顶部 `--ds-* → --dufe-*` 等设计系统语义事实不会因为计算结果等价而丢失；不同选择器、不同断点、简写/长写属性和权重无法静态证明的情况全部保留。对应合成用例覆盖同上下文覆盖、不同媒体查询、简写/长写和 `!important` 边界。
- 本切片移除 1,262 条必然失效的普通声明，三个源文件减少 40,485 字节；主构建 CSS 从上一切片的 272,024 字节降至 243,817 字节。验证：CSS 审计、全仓 Node 153/153、构建后渲染 4/4、相关 ESLint 与 `git diff --check` 通过；1280px 与 390×844 再次逐一复核五个主视图，资料/社区页与 Token 计算值，无横向溢出、抢焦点或浏览器页面错误。本切片未部署。

## M5 已验证切片：公共工作区 Masthead

- 新建 `PublicMasthead`，把资料检索、资料详情和校园回廊重复的品牌字标、导航、活动态与通知动作收敛为一个组件；管理员值守台仍使用独立顶栏，避免把公共浏览语境误套到高权限工作区。
- 公共顶栏统一使用纸墨朱 Token、78px/68px 桌面与手机节奏及 44px 操作目标；手机只保留当前任务和必要登录/通知入口，次要跨区链接仍保留在 DOM 中并按明确规则收纳。资料与社区模块原有重复 `.siteHeader`、`.wordmark` 规则已删除。
- 新增结构回归，锁定 `aria-current`、导航标签、手机显隐契约、44px 操作目标及三个页面的共享组件接入。验证：全仓 Node 154/154、构建后渲染 4/4、CSS 审计、相关 ESLint 与 `git diff --check` 通过；浏览器在 1280px 与 390×844 复核资料检索、资料详情、社区及五个主视图，均无横向溢出或自动抢焦点，页面控制台无错误。未启动 auth-api 时社区显示既有离线重试状态，不作为真实账号/数据库 E2E 证据。本切片未部署。

## 下一步

1. 在可用 PostgreSQL 17 环境验证 `0001`—`0012` 空库、升级、重复迁移、邮箱/TOTP/恢复码并发重放、社区证据快照、审核可逆流程、审计事务回滚和 migrator/runtime 权限，并补两个普通用户与一个管理员的真实浏览器 E2E。
2. 生产邮件发送适配器和校园账号验证仍待凭据/上游能力；保持失败关闭，不阻塞 M2 开发。
3. 继续 M5：人工归并自动审计无法证明等价的 V14—V16 剩余规则，下一批统一按钮、表单和弹层原语；每批必须构建并复核五个主视图。
4. M3 等待用户提供教师评价与课程教材完整版；收到前不从姓名或课程学院猜测教师身份。
