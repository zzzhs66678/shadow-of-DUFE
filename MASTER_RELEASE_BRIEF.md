你现在负责对 GitHub 仓库 `zzzhs66678/shadow-of-DUFE` 进行一次正式上线前的完整产品升级。

这不是一次简单的页面美化，而是一个包含用户系统、资料搜索、教师评价社区、通知系统、后台管理、视觉设计、安全加固、移动端适配和正式部署的综合项目。

请先完整检查现有仓库、页面结构、技术栈、数据库、部署方式和已有功能，再开始修改。不要未经检查就推翻现有代码，也不要重复实现仓库里已经存在的能力。

## 一、总目标

将 Shadow of DUFE 建设成一个适合东北财经大学学生使用的校园学习与信息社区，主要包括：

1. 用户注册、登录和跨设备数据同步。
2. 清晰直接的课程资料搜索体验。
3. 以教师为核心索引的教师评价系统。
4. 类似校园树洞与知乎讨论区的社区互动能力。
5. 统一、原创、具有游戏感的视觉和交互系统。
6. 安全、私密、可审计的管理员后台。
7. 完整的移动端适配、测试、部署和回滚能力。

最终结果必须是真实可运行的功能，而不是静态页面、假按钮、演示数据拼接或只有前端没有后端的半成品。

## 二、执行原则

### 1. 先审计，再设计，再开发

首先完成以下检查：

* 阅读 README、package.json、环境变量示例、数据库结构和部署配置。
* 启动现有项目，检查所有主要页面。
* 确认前端框架、后端框架、数据库、认证方式和云端部署环境。
* 找到“资料搜索”“选择教学班”“教师索引”“我的课表”“今日学习台”“空教室”等页面对应的源码。
* 检查仓库是否存在未提交修改，不得覆盖或删除用户已有成果。
* 建立当前功能清单和缺陷清单。

在 `docs/` 中保存简洁的设计和技术决策记录，但不要输出冗长的隐性推理过程。

### 2. 多角色评审

如当前环境支持子 Agent，请分别使用以下角色进行评审：

* 产品经理：梳理用户路径和功能优先级。
* UX 设计师：检查信息架构和交互复杂度。
* 视觉设计师：建立统一视觉系统。
* 游戏 UI 设计师：研究如何加入游戏感而不影响效率。
* 前端工程师：负责组件、响应式和性能。
* 后端工程师：负责认证、同步、社区、通知和后台。
* 安全工程师：进行威胁建模和安全审计。
* QA 工程师：设计自动化测试和人工验收流程。

若不支持真实子 Agent，则分别从上述角色视角完成独立审查，再综合形成最终方案。不要为了表现“多 Agent”而虚构不存在的执行结果。

### 3. 不因非关键阻塞停止整体开发

除非缺少无法替代的生产权限或密钥，否则不要停留在提问阶段。

对于缺失信息：

* 使用与现有技术栈兼容的合理默认方案。
* 将可配置内容放入环境变量。
* 提供 `.env.example`，绝不提交真实密钥。
* 明确记录需要用户补充的生产凭据。
* 不得伪造微信登录、云端部署或安全测试已经成功。

如果微信开放平台资质、AppID、AppSecret 或回调域名暂时不可用，先完成完整认证架构和至少一种可用的替代注册方式，不得让整个用户系统因此停滞。

## 三、工作流与提交方式

将项目拆分为以下六个工作流，每个工作流都视为独立的大项目，但最终必须整合为同一个产品。

1. 用户系统与后台。
2. 课程资料搜索体验。
3. 教师评价与校园社区。
4. 游戏化视觉与文案系统。
5. 安全、移动端、性能与测试。
6. 云端部署和 GitHub 提交。

每个工作流均需：

* 先检查现状。
* 编写实施计划。
* 完成功能开发。
* 增加必要测试。
* 修复测试发现的问题。
* 进行一次产品、UI 和安全复查。
* 创建语义明确的 Git commit。

不要在所有修改完成后只创建一个无法审查的超大提交。

## 四、工作流一：用户注册、登录、同步与管理员后台

### 目标

用户至少可以通过一种真实可用的方式注册和登录，并在不同设备登录同一账号后看到一致的数据。

### 认证方式

优先实现以下结构：

1. 微信扫码登录：

   * 使用合规的微信开放平台网页授权或扫码登录能力。
   * 后端完成 OAuth 回调、state 校验、账号创建和绑定。
   * 不得在前端暴露 AppSecret。
   * 不得用伪二维码冒充微信登录。

2. 必须同时准备至少一种可用的降级注册方式：

   * 邮箱验证码或 Magic Link；或
   * 用户名、邮箱和密码注册。

选择与现有技术栈最兼容、部署成本合理的方式。

若使用密码：

* 使用 Argon2id 或 bcrypt 安全哈希。
* 禁止明文保存密码。
* 增加登录限流、失败次数控制和安全会话管理。
* 提供注销、会话失效和密码重置能力。

### 注册流程

注册页面应简洁、清晰，用户可以理解每个字段的用途。

用户资料至少包括：

* 唯一用户 ID。
* 用户名。
* 头像。
* 学校账号或学号字段。
* 注册时间。
* 最后登录时间。
* 账号状态。
* 用户角色。

要求：

* 用户名长度和字符限制。
* 学校账号格式校验。
* 重复账号检查。
* 头像文件类型、大小、尺寸和内容安全校验。
* 默认头像和上传失败降级方案。
* 明确的隐私提示。
* 不收集完成产品功能不需要的信息。
* 错误提示应告诉用户如何修正，而不是只显示“请求失败”。

不得直接将“学校账号”当作可信身份认证依据。若没有学校官方认证接口，只能将其视为用户填写的信息，并在数据库中保留后续验证状态。

### 本地与云端同步

设计合理的数据同步机制：

* 云端数据库作为账号数据和跨设备数据的权威来源。
* 浏览器本地使用 IndexedDB 或适合现有框架的本地缓存。
* 用户未登录时可以保存必要的本地数据。
* 登录后将本地数据与云端账号合并。
* 明确定义冲突处理策略，例如以更新时间为准或提示用户选择。
* 不要把敏感认证信息保存在 localStorage。
* 换设备登录后应恢复用户可同步的数据。
* 退出登录后正确清理敏感本地状态。

### 管理员后台

建立只有站长本人可以访问的管理员后台，普通用户不能看到入口、路由和数据。

后台至少包含：

* 注册用户总数。
* 新增用户趋势。
* 用户列表。
* 用户名、注册时间、最后登录时间和账号状态。
* 搜索和筛选。
* 禁用、恢复用户。
* 查看用户公开资料。
* 评论管理。
* 举报处理。
* 内容删除或隐藏。
* 操作审计日志。

安全要求：

* 采用服务端角色权限判断，不能只靠前端隐藏按钮。
* 管理员账号与普通账号严格区分。
* 管理员后台建议支持 TOTP 二次验证。
* 可根据部署环境增加 IP 白名单，但不能把它作为唯一安全措施。
* 所有管理操作写入不可由普通用户修改的审计日志。
* 管理后台不得被搜索引擎索引。
* 不得把管理员密码、管理密钥或固定 Token 写进前端代码。
* 管理员统计接口不得向普通用户开放。

“只有我一个人看到”应通过认证、授权、路由保护和服务端校验实现，而不是依赖一个隐蔽网址。

### 验收标准

* 新用户可以完成真实注册。
* 用户可以登录和退出。
* 相同账号在两台设备上可以读取一致的云端数据。
* 未登录本地数据在登录后可以合理合并。
* 普通用户无法访问管理员接口。
* 管理员可以查看用户数量、管理用户和处理评论。
* 数据库迁移、种子数据和环境变量说明完整。
* 认证相关自动化测试通过。

## 五、工作流二：课程资料搜索体验重构

### 核心问题

当前用户搜索课程、教师或资料后，会被带到“选课”或“选择教学班”流程。但用户进入这个页面的目的只是寻找资料，因此不应被迫完成与获取资料无关的操作。

### 新交互原则

资料搜索页面只围绕“找到资料”设计。

搜索结果应直接展示：

* 资料标题。
* 对应课程。
* 对应教师。
* 资料类型。
* 上传时间。
* 文件大小或格式。
* 简短描述。
* 查看、预览或下载入口。

用户点击结果后，应进入资料详情或直接查看资料，而不是跳转到选课页面。

### 搜索逻辑

支持以下搜索维度：

* 课程名称。
* 教师姓名。
* 资料标题。
* 资料标签。
* 文件类型。
* 学期或年份。

课程和教师可作为筛选条件，但不能强迫用户先选择教学班。

删除或隐藏所有与资料获取无关的步骤。若某份资料确实依赖具体教学班，可以把教学班作为结果中的辅助信息，而不是整个搜索的必经流程。

### 空状态和异常状态

设计以下状态：

* 首次进入页面。
* 正在搜索。
* 没有结果。
* 网络错误。
* 文件失效。
* 权限不足。
* 多个同名课程或教师。

每个状态都应有清晰的下一步操作。

### 游戏化表达

可以将资料集合设计为“资料书架”“档案柜”或其他原创视觉隐喻，但必须保留：

* 明显的搜索框。
* 可读的结果列表。
* 键盘操作。
* 移动端触控。
* 无动画模式。
* 普通列表视图降级能力。

游戏化不得降低资料查找效率。

### 验收标准

* 搜课程、教师或资料时只返回相关资料。
* 点击资料不会进入选课流程。
* 用户最多通过两次主要操作打开目标资料。
* 手机端可以正常搜索和打开资料。
* 搜索、筛选、空状态和错误状态完整。
* 旧的有效资料链接尽量保持兼容。

## 六、工作流三：教师评价与校园社区

这是一个独立的大型功能，请按照真实社交产品设计，不能只做一个评论输入框。

### 教师索引

教师评价必须以稳定的 `teacher_id` 为核心，而不是以课程名称作为唯一索引，因为课程名称和授课安排可能变化。

设计数据结构时至少考虑：

* 教师 ID。
* 教师姓名。
* 所属学院。
* 历史授课课程。
* 教师别名或同名区分。
* 教师基础资料。
* 评价统计。
* 评价内容。
* 数据来源和更新时间。

用户后续会提供教师数据集，因此应建立：

* 数据导入脚本。
* 字段映射。
* 重复教师识别。
* 更新而非重复插入机制。
* 导入失败报告。
* 数据版本记录。

### 页面入口

在以下位置增加教师评价入口：

1. 教师索引页面。
2. 我的课表中每门课的教师信息附近。
3. 选课相关页面。
4. 今日学习台或空教室区域中的独立社区入口。

不同入口应进入同一套教师详情和评价数据，避免形成多个互不相通的系统。

### 教师详情页

教师详情页至少包括：

* 教师基础信息。
* 历史课程。
* 综合评价概览。
* 按时间、热度或相关性排序。
* 搜索评价。
* 标签或评价维度。
* 用户评价列表。
* 回复讨论。
* 发布评价入口。
* 举报入口。
* 使用规范。

评价维度应谨慎设计，避免诱导人身攻击。可以考虑：

* 课程组织。
* 内容清晰度。
* 作业与考核说明。
* 课堂互动。
* 资料完整度。

不要设计外貌、私人生活或其他与教学无关的评价维度。

### 社区讨论功能

参考知乎、Twitter/X、Reddit、北大树洞和上交水源等产品的信息组织思想，但不要复制它们的代码、素材、品牌或界面。

实现以下能力：

* 发布主题。
* 发布教师评价。
* 评论。
* 多级回复或合理的两级回复结构。
* 点赞。
* 收藏。
* 回复提醒。
* 被提及提醒。
* 系统通知。
* 删除自己的内容。
* 编辑记录或合理的编辑时间限制。
* 举报。
* 屏蔽用户。
* 内容折叠。
* 热门排序和时间排序。
* 分页或游标加载。
* 分享链接。
* 用户个人主页中的公开内容列表。

页面可以借鉴知乎评论区的信息密度和讨论层级，但应形成自己的视觉风格。

### 通知系统

至少支持：

* 有人回复我的主题。
* 有人回复我的评论。
* 有人提及我。
* 我的内容被处理。
* 管理员公告。

通知应具有：

* 未读数量。
* 已读状态。
* 单条已读。
* 全部已读。
* 点击后跳转到对应内容。
* 删除内容后的合理降级页面。

### 初始内容

用户后续可能提供爬取或整理的历史评论。

数据导入时必须：

* 只导入用户明确提供且有权使用的数据。
* 删除或匿名化个人敏感信息。
* 保留合理的来源字段。
* 防止重复导入。
* 过滤手机号、身份证号、私人联系方式等敏感内容。
* 不得编造真实学生身份或冒充真实用户发布评论。

项目初期可以加入明确标注为“示例内容”或“历史整理内容”的种子数据，但不能让用户误认为是当前真实注册用户发布。

### 内容治理和安全

社区功能必须包含：

* 服务端输入校验。
* HTML 清理和 XSS 防护。
* SQL 注入防护。
* CSRF 防护。
* 发布、点赞、回复和举报限流。
* 单用户和单 IP 异常频率检测。
* 内容长度限制。
* 链接限制。
* 文件上传限制。
* 敏感内容处理接口。
* 举报队列。
* 管理员删除、隐藏、恢复和封禁能力。
* 软删除和审计记录。
* 防刷赞和重复操作约束。
* 数据库唯一索引和事务保护。

不要仅依赖前端过滤。

### 验收标准

* 教师评价通过 teacher_id 关联。
* 同一教师在课表、教师索引和社区中展示同一份评价数据。
* 用户可以发布、回复、点赞、举报并收到通知。
* 管理员可以审核和处理内容。
* 删除内容后引用关系不会导致页面崩溃。
* 高频恶意请求受到限制。
* 移动端评论和回复体验可用。
* 社区接口和权限测试通过。

## 七、工作流四：原创游戏化视觉与全站文案

### 设计方向

现有网站部分字体、阴影、层级和文案不协调。希望网站具有《女神异闻录》式高对比、强构图、动感排版和游戏菜单感，但只能将其作为情绪和设计方法参考。

禁止：

* 复制《女神异闻录》的 Logo、角色、UI 素材、音乐、图标或受版权保护的视觉资产。
* 直接复刻其具体页面。
* 使用来源和授权不明的图片。
* 为了视觉效果牺牲可读性和功能。

最终应形成 Shadow of DUFE 自己的原创设计语言。

### 建立设计系统

先建立统一设计 Token：

* 字体层级。
* 字号。
* 行高。
* 字重。
* 间距。
* 圆角。
* 边框。
* 阴影。
* 动效时长。
* 层级关系。
* 主色、辅助色和状态色。
* 深色与浅色背景上的可读性规则。

重点修复：

* 过重或模糊的阴影。
* 多种无规律字体混用。
* 对比度不足。
* 卡片层级混乱。
* 按钮样式不统一。
* 动画过多。
* 移动端文字拥挤。
* 页面之间风格断裂。

### 游戏化交互

可以探索但不限于：

* 将资料集合设计成原创的数字书架或档案库。
* 将学习进度设计为任务日志。
* 将今日学习台设计为指挥台或行动面板。
* 将空教室查询设计为地图节点或空间探索。
* 将通知设计为通讯终端。
* 将个人资料设计为学生档案卡。
* 将课程和教师关系做成可探索的信息网络。
* 使用适度的转场、反馈和成就感设计。

所有创意必须满足：

* 用户可以立刻理解如何操作。
* 核心功能始终有传统控件兜底。
* 支持 `prefers-reduced-motion`。
* 动画不会阻塞点击。
* 低性能手机可以流畅使用。
* 页面加载失败时仍可使用基础功能。
* 视觉效果不能干扰屏幕阅读器和键盘操作。

### 文案审计

审查网站上所有用户可见文字。

删除：

* 开发者口吻。
* 直接复制的需求描述。
* “这里以后做某功能”之类占位文案。
* 对普通用户没有意义的技术术语。
* 过度中二、过度营销或不明确的表达。

重新编写：

* 页面标题。
* 按钮。
* 表单提示。
* 错误信息。
* 空状态。
* 成功提示。
* 引导文案。
* 注册说明。
* 隐私说明。
* 社区规范。
* 管理员提示。

文案可以有适度的艺术感和世界观，但用户必须一眼理解功能。不要在每句话里加入游戏化表达。

### 素材要求

只允许使用：

* 自制素材。
* 明确可商用的开源素材。
* 已获得授权的素材。
* CSS、SVG 或程序生成的原创视觉元素。

保存素材来源和许可证说明。

### 验收标准

* 全站使用统一设计 Token。
* 不再存在明显随意的字体和阴影。
* 所有主要页面视觉统一。
* 核心功能比改版前更容易使用。
* 动效可以关闭或自动降级。
* 达到合理的无障碍对比度。
* 不包含侵权或来源不明素材。
* 用户文案不再暴露开发提示词。

## 八、工作流五：安全、移动端、性能和质量保证

### 安全审计范围

由安全角色对以下内容进行完整检查：

* 身份认证。
* 会话管理。
* 权限控制。
* 管理员后台。
* 数据库访问。
* 用户输入。
* 文件上传。
* API 限流。
* 社区滥用。
* 通知系统。
* 日志。
* 错误信息。
* 环境变量。
* 第三方依赖。
* 部署配置。
* HTTP 安全响应头。

至少检查：

* XSS。
* CSRF。
* SQL 注入。
* IDOR。
* 越权访问。
* SSRF。
* 开放重定向。
* 路径遍历。
* 文件类型伪造。
* 暴力破解。
* 用户枚举。
* Mass Assignment。
* 敏感信息泄露。
* 依赖漏洞。
* 管理接口暴露。
* 日志中泄露 Token 或密码。

根据技术栈配置合理的：

* CSP。
* HSTS。
* Secure、HttpOnly、SameSite Cookie。
* CORS 白名单。
* 请求体大小限制。
* 上传文件白名单。
* 速率限制。
* 数据库最小权限。
* 生产环境错误隐藏。
* 备份与恢复策略。

### 移动端

重点测试常见手机宽度：

* 320px。
* 360px。
* 375px。
* 390px。
* 414px。
* 平板宽度。

确保：

* 注册和登录可用。
* 搜索可用。
* 评论输入不被键盘遮挡。
* 回复层级不会无限缩窄。
* 导航不溢出。
* 弹窗可以关闭。
* 触控目标足够大。
* 不依赖鼠标悬停。
* 横屏和竖屏不会破坏关键功能。

### 性能

检查并优化：

* 首屏加载。
* 图片体积。
* 字体加载。
* 动画开销。
* 长评论列表。
* 搜索请求。
* API 重复调用。
* 数据库索引。
* 前端 bundle。
* 缓存策略。
* 懒加载和分页。

### 测试

至少包含：

* 单元测试。
* API 集成测试。
* 权限测试。
* 注册登录流程测试。
* 跨设备同步逻辑测试。
* 资料搜索测试。
* 教师评价测试。
* 评论和通知测试。
* 管理员操作测试。
* 移动端端到端测试。
* 构建测试。
* 数据库迁移测试。

对关键路径执行端到端测试：

1. 新用户注册。
2. 编辑头像和用户名。
3. 登录另一设备。
4. 搜索一门课程资料。
5. 打开资料。
6. 进入教师页面。
7. 发布一条评价。
8. 另一用户回复。
9. 原用户收到通知。
10. 管理员处理举报。
11. 普通用户尝试访问后台并被拒绝。

不要只运行格式化和 TypeScript 编译就声称测试完成。

## 九、工作流六：部署、GitHub 和上线准备

### 部署前要求

只有在以下项目通过后才能部署：

* 项目构建成功。
* 数据库迁移成功。
* 核心自动化测试通过。
* 管理员权限验证通过。
* 生产环境密钥未进入仓库。
* 安全审计中的高危问题已经修复。
* 移动端关键页面通过检查。
* 具备回滚方案。

### 云端部署

根据现有项目的部署方式完成：

* 前端部署。
* 后端部署。
* 数据库迁移。
* 对象存储或头像存储。
* 环境变量配置。
* HTTPS。
* 域名和回调地址。
* 日志和错误监控。
* 健康检查。
* 数据备份。

无法获得生产凭据时：

* 完成可部署配置。
* 提供明确部署命令和变量清单。
* 在本地或可用测试环境验证。
* 不得声称已经部署到生产环境。

### GitHub

目标仓库：

`https://github.com/zzzhs66678/shadow-of-DUFE`

要求：

* 检查远程仓库和当前分支。
* 保留用户已有提交历史。
* 不使用强制推送覆盖历史。
* 不提交 `.env`、数据库密码、微信密钥、管理员密钥或云平台密钥。
* 提交数据库迁移。
* 更新 README。
* 更新 `.env.example`。
* 增加部署和回滚说明。
* 增加管理员初始化说明。
* 增加素材许可证说明。
* 将修改推送到合适分支。
* 若不应直接修改主分支，则创建发布分支或 Pull Request。
* 清楚报告实际推送结果，不得伪造成功。

## 十、最终 Definition of Done

只有以下项目全部满足，才可以宣布任务完成：

* 至少一种注册方式真实可用。
* 微信扫码登录架构完成；缺少资质时有明确配置说明和可用替代登录。
* 用户数据可以在云端保存并跨设备恢复。
* 本地缓存和云端同步逻辑明确。
* 管理员可以查看用户数量并管理用户。
* 普通用户无法访问管理员后台。
* 资料搜索结果只围绕资料，不再强制跳转选课。
* 教师评价以 teacher_id 为核心。
* 课表、教师索引和社区使用统一教师数据。
* 用户可以发布、回复、点赞、举报和接收通知。
* 管理员可以处理不合适内容。
* 社区具备服务端限流和安全防护。
* 全站形成统一原创视觉系统。
* 用户文案全部经过审计。
* 不使用侵权或来源不明素材。
* 手机端核心功能可用。
* 所有高危安全问题已修复。
* 核心测试通过。
* README、部署文档和环境变量说明完整。
* GitHub 中存在可审查的提交记录。
* 云端实际部署成功，或者明确列出唯一剩余的外部凭据阻塞项。

## 十一、最终回复格式

完成工作后，按以下结构汇报：

1. 仓库原有架构和发现的问题。
2. 最终产品与技术方案。
3. 每个工作流实际完成的功能。
4. 主要修改文件。
5. 数据库迁移和数据模型。
6. 注册、同步和管理员后台说明。
7. 社区和通知系统说明。
8. 视觉系统和文案改造说明。
9. 已执行的测试及结果。
10. 安全审计发现和修复。
11. 移动端和性能检查结果。
12. 部署地址和健康状态。
13. Git 分支、commit 和 Pull Request 信息。
14. 尚未完成或受外部凭据阻塞的事项。

必须区分：

* 已真实完成并验证。
* 已实现但需要生产凭据。
* 仅设计尚未实现。
* 测试失败或无法验证。

禁止为了满足“全部完成”的要求隐瞒失败、伪造结果、使用静态假数据替代真实功能，或者把尚未实现的功能描述为已完成。

现在开始：先检查整个仓库和运行现有项目，建立基线，再依次完成六个工作流。除不可替代的生产凭据外，不要因为一般性问题停止开发。
