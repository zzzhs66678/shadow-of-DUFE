# 复制到服务器 /srv/apps/dufesh/shared/config/postgres.env 并设置 chmod 600。
# 不要把真实密码提交到 Git。
POSTGRES_DB=dufesh
POSTGRES_USER=dufesh_app
POSTGRES_PASSWORD=replace-with-a-long-random-password
PGDATA=/var/lib/postgresql/data/pgdata
